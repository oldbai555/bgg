# 前端开发进度与实现方案

> 本文档是前端开发的历史/参考日志：记录已完成什么、还差什么、关键决策为什么这么定，以及代码索引。开发规范类内容已迁移至 Cursor 规则文件，不在本文档重复。

---

## 0. 项目概述与技术栈

**admin-frontend**：基于 Vue 3 + TypeScript + Vite 构建的前端管理系统，包含后台管理功能（RBAC、系统支撑、聊天、日志等）和公共展示功能（博客、视频）。

**核心技术栈**：
- Vue 3.4+（Composition API）、Vite 5.0+、TypeScript 5.3+
- Pinia 2.1+（状态管理）、Vue Router 4.2+
- Axios 1.6+（HTTP）、Element Plus 2.4+（UI）、vue-i18n 9.9+（国际化）
- dplayer 1.27+（视频播放；`video.js` 曾在依赖清单里但全仓无实际引用，2026-07 Phase 1 重构期间确认后移除）
- vitest 2.1+（单测，2026-07 Phase 1 引入，见 §4）

**目录结构要点**（详见 `.cursor/rules/20-frontend.mdc`；2026-07 Phase 1-2 按后端 9 业务域重组，过程记录见 `admin-frontend/docs/progress.md`）：
- `src/api/generated/`：goctl 从后端 `.api` 生成的接口代码（禁止手改）
- `src/api/{iam,system,monitoring,misc,content,chat,sdk,task}.ts`：8 个域各一个手写二次封装（`content.ts` 合并了旧 `blog.ts`+`video.ts`），另有 `public.ts`；视图禁止直接 import `generated/` 里的请求函数，类型 `import type` 例外
- `src/components/common/D2Table.vue`：统一表格组件（列表+分页+详情/编辑+新增），已泛型化（`<script setup lang="ts" generic="T">`）
- `src/stores/dict.ts`：全局字典状态；`src/composables/useDictOptions.ts`：字典选项 composable；`src/stores/websocket.ts`（连接生命周期）+ `src/stores/notification.ts`（未读消息列表，订阅前者）
- `src/views/{iam,system,monitoring,misc,content,chat,sdk,task}/`：后台业务页面，按后端业务域分目录（`content` 合并了旧 `blog`/`video`，`chat` 即旧 `chatroom`）
- `src/views/public/`：公共（无需登录）展示页面
- `src/styles/public-list.scss`、`src/styles/public-detail.scss`：公共页面通用样式模板
- `src/composables/`：`hooks/` 目录已于 2026-07 Phase 1 合并进来，不再新建 `hooks/`

**前后端协同**：后端定义 `.api` → goctl 生成后端 → goctl 生成前端 TS（`scripts/generate-ts.sh`）→ 前端二次封装 → 页面使用。

**部署**：`pnpm dev`（本地代理 `/api` → `localhost:20000`）；`pnpm build` 输出 `dist/`，部署路径 `/admin/`，生产环境通过 Nginx 代理网关。

开发流程、API/字典/组件使用规范已迁移至 `.cursor/rules/00-workflow.mdc`、`.cursor/rules/20-frontend.mdc`、`.cursor/rules/21-public-pages.mdc`，请查阅规则文件。

---

## 1. 核心功能索引

- **认证与登录**：`src/views/Login.vue`、`src/stores/user.ts`、`src/utils/request.ts`
- **权限管理**：路由级（`router/index.ts` meta.permission）+ 按钮级（`v-permission` 指令，`src/directives/permission.ts`）+ `src/composables/usePermission.ts`
- **菜单管理**：`src/stores/user.ts`（menus，本地缓存 TTL 5min）+ `src/components/layout/AppSidebar.vue` + 动态路由注入
- **数据字典**：`src/stores/dict.ts` + 登录后批量加载（`POST /api/v1/dict/batch`）+ `useDictOptions`
- **通用组件**：`D2Table`（表格，已泛型化）、`ImageUpload`、`NoticeReader`
- **主题与国际化**：`src/styles/theme.scss`（`data-theme` 切换）、`src/i18n/index.ts`
- **移动端适配**：断点定义于 `src/styles/variables.scss`
- **WebSocket 与未读消息**：`src/stores/websocket.ts`（连接生命周期，登录后自动连接）+ `src/stores/notification.ts`（未读消息列表/已读状态，订阅 `websocket.ts` 的 `lastMessage`），用于聊天/实时通知

---

## 2. 已完成功能（历史记录）

- 初始化项目骨架（Vite + Vue3 + TS + Pinia + Router + Element Plus），登录、基础布局、菜单导航、Token 拦截。
- **阶段三 RBAC**：角色管理（`RoleList.vue`）、权限管理（`PermissionList.vue`）、部门管理（`DepartmentList.vue`，树形）、菜单管理（`MenuList.vue`，树形）、用户管理（`UserList.vue`），均为列表+分页+新增/编辑/删除。
- **阶段四 系统支撑**：系统配置（`ConfigList.vue`，支持刷新缓存）、数据字典类型/项管理（`DictTypeList.vue`/`DictItemList.vue`）、文件管理（`FileList.vue`，含上传/下载），均已接入 D2Table。
- **demo 功能页面**（已废弃）：`DemoList.vue` 曾作为开发流程示例，由脚本自动生成骨架，展示标准 CRUD + 权限绑定写法；2026-07 Phase 1 死代码清理确认零引用后删除，见 `admin-frontend/docs/progress.md`。
- **阶段七 聊天功能**：会话列表（`ChatList.vue`）、消息列表（`ChatMessageList.vue`）、群组管理（`ChatGroupList.vue`，含成员管理），WebSocket 实时收发与在线状态。
- **阶段八 公告与通知**：公告管理（`NoticeList.vue`，支持定时发布）、消息通知（`NotificationList.vue`，已读/全部已读/清除/删除）。
- **阶段九 性能日志**：`PerformanceLogList.vue`，支持按方法/路径/慢接口/状态码/时间范围筛选。
- **阶段十 视频管理**：`VideoList.vue`（列表管理）+ `VideoPlayer.vue`（video.js 播放器，支持 m3u8/mp4，智能代理播放：先直连失败后走代理）。
- **阶段十一 异步任务**：`TaskList.vue`（任务详情、参数/结果 JSON、取消任务）、`TaskFloatBall.vue`（浮动任务球，实时状态、完成任务可直接下载）；操作/登录/审计/SDK调用/性能日志的导出功能统一改为创建异步任务，不再直接触发 blob 下载。
- **SDK 管理（首期）**：SDK API Key 管理（列表/新增/编辑/删除、IP 白名单、过期时间戳），权限绑定 `sdk_key:*`。
- **数据统计**：`MetricStats.vue`，按 module/bizId/day 查询 PV/UV/VV/IP，数值卡片 + 柱状对比图。
- **博客功能模块**（详见 §6.1）：标签/文章/审核管理 + 公共博客列表/详情页 + 打点上报。
- **博客扩展功能**（详见 §6.2）：友情链接、社交信息、文章置顶、标签筛选。
- **Public 页面小程序风格改造**（详见 §6.3）：博客、视频公共页统一视觉与交互，含移动端滚动位置恢复。

---

## 3. 待实现 / 待完善功能

- [ ] 菜单组件路径与后端约定的自动映射策略（当前手工 map）。
- [ ] 主题更多维度（色板、字体），国际化文案进一步细化。
- [ ] 单元测试与 E2E 验证（含 `BlogArticleEdit.vue` 状态流转、`MetricReporter.vue`、`MetricStats.vue` 图表计算等用例）。
- [ ] SDK 接口管理页面：`/api/v1/sdk/interface/*` 的前端管理（默认限频、状态配置）。
- [ ] SDK 调用记录页面：`/api/v1/sdk/call/log/list` 列表 + 按 key/apiCode/IP/状态码/时间筛选 + 导出。
- [ ] 博客标准风格改造（顶部导航、目录 TOC、阅读时间/字数统计、相邻文章导航等）曾有详细方案设计，尚未实施；如重启该方向，需重新评估是否仍然需要（见 §4 决策记录）。
- [ ] 移动端细节持续打磨（Markdown 渲染区域内边距、滚动位置恢复等，已在博客/视频公共页面部分实现，可推广到更多页面）。

---

## 4. 技术决策记录

- 2025-12-24：前端使用 goctl TS 生成 + Axios 二次封装；对生成代码中的 path param 问题在自定义 API 层修正（不修改 generated）。
- 2025-12-24：权限指令 `v-permission` 基于 Pinia 权限列表与 `permissions.includes('*')`；路由 `meta.permission` 校验不通过跳转 403。
- 2025-12-24：权限/菜单本地缓存 TTL 5 分钟；动态路由由菜单树驱动并开启 KeepAlive；主题切换通过 `data-theme`。
- 2026-01-13：导出功能统一走「异步任务 + 文件管理」模式：导出按钮只创建异步任务，不直接请求 blob 下载；任务完成后通过任务详情结果 JSON（如 `fileUrl`）获取下载链接，由文件管理模块统一承载下载逻辑。
- 曾尝试将 Public 页面迁移到独立的 admin-nuxt（Nuxt3 SSR）项目，已于后续版本完全回滚合并回 admin-frontend，不再重复此方向。
- 博客「标准博客风格」改造（顶部导航栏、左侧分类导航、右侧 TOC 目录、阅读时间/字数统计等）曾完成详细设计评审，但未实施；当前 Public 页面维持「小程序风格」（见 `.cursor/rules/21-public-pages.mdc`），该方向搁置，不再维护详细设计稿。
- 2026-07：admin-frontend Phase 1-2 架构重构（域目录按后端 9 域重组、API wrapper 全覆盖、D2Table 泛型化、websocket/notification store 拆分，完整过程见 `admin-frontend/docs/progress.md`）期间发现并修复的几处**真实功能行为 bug**（均为重构前就存在，不是本轮引入）：公共页（`/admin/blog/...`）游客遇到 10003 过期码时因 `isPublicPath()` 路径判断未剥离 `/admin` 前缀，在生产环境永远被误判为需要登录并强制跳转，实际已改成读路由解析后的 path；`NoticeList.vue` 编辑抽屉的下拉选项此前赋值成 `ComputedRef` 未取 `.value`，抽屉里公告类型/状态下拉一直渲染不出选项；`ChatMessageList.vue` 的"接收用户"筛选列/`toUserName` 字段从未在后端生效（真实字段是 `chatId`），已改用 `chatId` 筛选并删除该列；`D2Table.vue` 的 el-pagination 同时监听 `v-model` 与显式 `@current-change`/`@size-change`，一次翻页/改页大小会让父页面收到两次事件、重复请求列表接口，已去重为只走 `v-model`。

---

## 5. 关键代码位置

> 2026-07 Phase 1 域目录重组后，以下路径均为重组后的现状；重组过程与逐文件迁移清单见 `admin-frontend/docs/progress.md`、`admin-frontend/docs/02-domain-reorg-and-api-layer.md`。

- 入口/路由/布局：`src/main.ts`、`src/router/index.ts`、`src/layouts/DefaultLayout.vue`、`src/views/Login.vue`
- 用户状态/请求封装：`src/stores/user.ts`、`src/utils/request.ts`
- RBAC 页面：`src/views/iam/{RoleList,PermissionList,DepartmentList,MenuList,UserList,ApiList}.vue`
- 系统支撑页面：`src/views/system/{ConfigList,DictTypeList,DictItemList,FileList,NoticeList,NotificationList}.vue`
- 聊天功能：`src/views/chat/ChatList.vue`（238 行，2026-07 拆分出 `src/components/chat/{ChatListItem,ChatMessageBubble,ChatMessageInput}.vue` + `src/composables/useChatList.ts`）
- 监控/日志页面：`src/views/monitoring/{AuditLogList,LoginLogList,OperationLogList,PerformanceLogList,MonitorList,MetricStats}.vue`
- 视频管理：`src/views/content/{VideoList,VideoPlayer}.vue`
- 任务管理：`src/views/task/TaskList.vue`；浮动任务球：`src/components/common/TaskFloatBall.vue`；WebSocket 连接：`src/stores/websocket.ts`；未读消息：`src/stores/notification.ts`
- 数据统计：`src/views/monitoring/MetricStats.vue`；API：`src/api/monitoring.ts`
- 博客后台：`src/views/content/{BlogTagList,BlogArticleList,BlogArticleEdit,BlogArticleAuditList,BlogFriendLinkList,BlogSocialInfoList}.vue`
- 博客公共页：`src/views/public/{BlogList,BlogDetail}.vue`；视频公共页：`src/views/public/{VideoList,VideoDetail}.vue`
- 通用打点组件：`src/components/common/MetricReporter.vue`；ICP 备案：`IcpFooter` 组件
- 博客/视频 API 封装：`src/api/content.ts`（合并了旧 `blog.ts`+`video.ts`）
- goctl 生成目录：`src/api/generated/`；生成脚本：`scripts/generate-ts.sh`
- 权限指令/composable：`src/directives/permission.ts`、`src/composables/usePermission.ts`（原 `src/hooks/usePermission.ts`，已合并进 `composables/`）
- 错误页：`src/views/error/{NoAccess,NotFound}.vue`
- 主题与 i18n：`src/styles/theme.scss`、`src/i18n/index.ts`
- 公共页面样式模板：`src/styles/public-list.scss`、`src/styles/public-detail.scss`
- 单测：`vitest`，`npm run test`；`src/stores/*.spec.ts`、`src/composables/*.spec.ts`、`src/components/common/D2Table.spec.ts` 等，覆盖范围与优先级见 `admin-frontend/docs/03-state-management-and-testing.md`

接口对接状态以生成的 API 客户端（`src/api/generated/`）为准，不在本文档维护手工接口清单。

---

## 6. 已实现功能模块设计要点（简史）

### 6.1 博客功能模块
- 后台：标签管理、文章管理（列表+独立 Markdown 编辑页 `BlogArticleEdit.vue`，含状态流转与只读/可编辑控制、封面与正文图片上传）、文章审核（通过/驳回/审核下架，支持审核意见）。
- 公共页：`BlogList.vue`（卡片列表+搜索分页+PV/UV打点）、`BlogDetail.vue`（详情+打点），均接入 `MetricReporter` 组件与 `IcpFooter`，并做过移动端排版与滚动位置恢复优化。
- 字典：`blog_article_status`、`blog_article_audit_status` 已加入 `REQUIRED_DICT_CODES`。
- 路由：后台路由由菜单动态注入；公共路由 `/public/blog`、`/public/blog/:id` 在路由守卫中列入免登录白名单。
- 代码位置：`src/views/content/`（原 `src/views/blog/`，2026-07 Phase 1 合并进 content 域）、`src/views/public/Blog*.vue`、`src/api/content.ts`（原 `src/api/blog.ts`）。

### 6.2 博客扩展功能（友情链接 / 社交信息 / 置顶 / 标签筛选）
- 后台新增：`BlogFriendLinkList.vue`、`BlogSocialInfoList.vue`（均为 D2Table CRUD，字段长度与后端一致：名称≤15中文字符、链接≤255字符、备注≤127中文字符）。
- 公共列表页扩展：标签筛选（单选，`publicBlogTagList`）、置顶标识（`isTop` 字段，红色标签+样式高亮）、右侧侧边栏（移动端移至底部）展示友情链接与社交信息（图标匹配 GitHub/Gitee/邮箱等，新窗口打开）。
- 文章管理页新增置顶/取消置顶操作与列表置顶标识。
- 代码位置：`src/views/content/{BlogFriendLinkList,BlogSocialInfoList}.vue`、`src/views/public/BlogList.vue`、`src/api/content.ts`。

### 6.3 Public 页面风格
- 当前统一采用「小程序风格」（暖色渐变背景、卡片式列表/详情、简化移动端分页），样式契约已迁移至 `.cursor/rules/21-public-pages.mdc`，不在本文档重复。
- 已应用页面：博客列表/详情页、视频列表/详情页，均复用 `public-list.scss` / `public-detail.scss` 通用模板。
- 曾评审过「标准博客风格」大改造方案（顶部导航栏+左侧分类导航+右侧 TOC 目录+阅读时间/字数统计+相邻文章导航等，含 `BlogHeader.vue`/`BlogTOC.vue` 等新组件设计），未实施，见 §4 决策记录。

---

## 7. DDD-lite 重构后前端联调（2026-07-07）

后端 DDD-lite 重构**未改变 HTTP 路径**，现有 `src/api/generated/` 与 `blog.ts` 等封装层可继续用。

**联调步骤**（详见 [`docs/admin-server-ddd-smoke-test.md`](admin-server-ddd-smoke-test.md) 第三节）：

1. 后端 `go build ./...` + 本地启动验证接口
2. **用户执行** `admin-server/scripts/generate-ts.sh`（仅当 `admin.api` 有变更时需要；当前对比结论：169 函数名 / 168 路径与现网一致，可不重生成）
3. `npm install` → `npm run typecheck` → `npm run dev`
4. 走查：登录 → Blog 后台 CRUD → `/blog/` 公开页 → 异步导出任务浮球

**注意**：`package.json` 的 `api:gen` 脚本已失效并于 2026-07 Phase 1 删除，唯一生成入口是 `generate-ts.sh`。
