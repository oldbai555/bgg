# 前端开发进度与实现方案

> **本文档是前端开发的唯一指导文档**，整合了项目架构、技术栈、开发规范、功能进度等所有内容。

---

## 0. 项目概述与技术栈

### 项目概述
本项目是基于 Vue 3 + TypeScript + Vite 构建的现代化前端管理系统，采用组件化、模块化设计，支持权限管理、主题切换、国际化等功能。

### 技术栈

#### 核心框架
- **Vue 3.4+**: 渐进式 JavaScript 框架，使用 Composition API
- **Vite 5.0+**: 下一代前端构建工具，提供极速的开发体验
- **TypeScript 5.3+**: 类型安全的 JavaScript 超集

#### 状态管理
- **Pinia 2.1+**: Vue 3 官方推荐的状态管理库

#### HTTP 请求
- **Axios 1.6+**: Promise 基础的 HTTP 客户端

#### UI 组件库
- **Element Plus 2.4+**: 企业级 UI 组件库
- **Element Plus Icons**: 图标库

#### 路由管理
- **Vue Router 4.2+**: Vue 官方路由库

#### 国际化
- **vue-i18n 9.9+**: Vue 国际化解决方案

#### 其他工具库
- **video.js 8.23+**: 视频播放器（用于视频管理功能）

### 项目结构

```
admin-frontend/
├── src/
│   ├── api/                        # API 请求层
│   │   ├── generated/              # goctl 生成的接口函数（禁止手改）
│   │   │   ├── admin.ts            # 后端 admin.api 生成的 TS 代码
│   │   │   ├── adminComponents.ts  # 类型定义组件
│   │   │   └── gocliRequest.ts     # 请求工具
│   │   └── public.ts               # 公共接口封装（如公共字典）
│   ├── components/                  # 通用组件库
│   │   ├── common/                 # 通用业务组件
│   │   │   ├── D2Table.vue         # 统一表格组件（列表+分页+详情/编辑+新增）
│   │   │   ├── ImageUpload.vue     # 图片上传组件
│   │   │   └── NoticeReader.vue    # 公告阅读器
│   │   └── layout/                 # 布局组件
│   │       ├── AppHeader.vue       # 顶部导航
│   │       ├── AppSidebar.vue      # 左侧菜单
│   │       ├── Breadcrumb.vue      # 面包屑
│   │       └── ...
│   ├── composables/                # 组合式函数
│   │   ├── useAppConfig.ts         # 应用配置（如 baseURL）
│   │   └── useDictOptions.ts       # 字典选项管理
│   ├── directives/                 # 自定义指令
│   │   └── permission.ts           # 权限指令 v-permission
│   ├── hooks/                      # 组合式函数（hooks）
│   │   └── usePermission.ts        # 权限判断工具
│   ├── i18n/                       # 国际化资源
│   │   └── index.ts                # i18n 配置
│   ├── layouts/                    # 布局组件
│   │   └── DefaultLayout.vue       # 默认布局（主框架）
│   ├── router/                     # 路由配置
│   │   └── index.ts                # 路由主文件（包含动态路由注入）
│   ├── stores/                     # 状态管理 (Pinia)
│   │   ├── app.ts                  # 应用全局状态
│   │   ├── dict.ts                 # 字典状态（统一管理字典数据）
│   │   ├── user.ts                 # 用户状态（登录、权限、菜单）
│   │   └── websocket.ts            # WebSocket 状态（实时通信）
│   ├── styles/                     # 全局样式
│   │   ├── layout.scss             # 布局样式
│   │   ├── theme.scss              # 主题样式
│   │   └── variables.scss          # 样式变量（断点、颜色等）
│   ├── types/                      # TypeScript 类型定义
│   │   └── table.ts                # 表格相关类型（D2Table）
│   ├── utils/                      # 工具函数
│   │   ├── breadcrumb.ts           # 面包屑工具
│   │   ├── clipboard.ts            # 剪贴板工具
│   │   ├── date.ts                 # 日期格式化工具
│   │   ├── file.ts                 # 文件工具
│   │   └── request.ts              # HTTP 请求封装（Axios 拦截器）
│   ├── views/                      # 页面级组件
│   │   ├── chatroom/               # 聊天功能
│   │   ├── error/                  # 错误页
│   │   ├── public/                 # 公开页面（不需要登录）
│   │   ├── sdk/                    # SDK 管理
│   │   ├── system/                 # 系统管理
│   │   ├── temp/                   # 临时/示例页面
│   │   ├── video/                  # 视频管理
│   │   ├── Dashboard.vue           # 仪表盘/首页
│   │   └── Login.vue               # 登录页
│   ├── App.vue                     # 根组件（KeepAlive 包裹）
│   └── main.ts                     # 应用入口（全局配置、错误处理）
├── public/                         # 公共文件
├── vite.config.ts                  # Vite 配置（代理、别名等）
├── tsconfig.json                   # TypeScript 配置
└── package.json                    # 依赖管理
```

### 核心架构设计

#### 技术分层
```
┌──────────────────────────────────────┐
│         Page / View Layer            │  (views/*.vue)
├──────────────────────────────────────┤
│      Component Layer                 │  (components/*.vue)
├──────────────────────────────────────┤
│   State Management (Pinia)           │  (stores/*.ts)
├──────────────────────────────────────┤
│   API / Service Layer                │  (api/*.ts)
├──────────────────────────────────────┤
│   HTTP Client (Axios)                │  (utils/request.ts)
├──────────────────────────────────────┤
│   Backend API                        │  (go-zero 后端)
└──────────────────────────────────────┘
```

#### 前后端协同（goctl 生成）

**工作流程**：
```
后端定义 .api → goctl api go 生成后端 → goctl api ts 生成前端 → 前端在 api/ 手动二次封装 → 页面/业务使用
```

**目录约定**：
- `src/api/generated/`：goctl 生成的接口函数（**禁止手改**）
- `src/api/*.ts`：对 generated 代码的二次封装、错误处理、适配
- `admin-server/scripts/generate-ts.sh`：批量生成脚本

**生成步骤**：
1. 安装 goctl：`go install github.com/zeromicro/go-zero/tools/goctl@latest`
2. 单文件生成：`cd admin-server && ./scripts/generate-ts.sh admin.api`
3. 生成代码规范：
   - ✅ 必做：使用 goctl 从 .api 生成 TS 代码；生成文件仅存放 `generated/`
   - ✅ 必做：封装层统一错误处理；遵守 Page → Component → Store → API 分层
   - ❌ 禁止：手动编辑 `generated/` 下的代码；重新定义与 generated 重复的类型

---

## 1. 开发必读
- 遵循 Page → Component → Store → API → Backend 分层，generated 代码禁止手改。
- goctl 前后端协同：使用 `pnpm api:gen` 或直接执行 goctl 生成 TS（输出到 `src/api/generated/`），业务封装写在 `src/api/*.ts`。
- **API 时间字段规范（强制）**：
  - 后端统一返回 `int64` 类型的时间戳（秒级），不做格式化
  - 前端负责时间格式化显示，使用统一的工具函数（如 `formatTime`, `formatDateTime` 等）将时间戳转换为可读的日期时间字符串
  - 所有涉及时间字段（`createdAt`, `updatedAt`, `deletedAt`, `publishTime`, `readAt` 等）的 API 响应都是 `number` 类型（对应后端的 `int64`）
- **开发流程规范（强制遵守）**：
  1. **确定要开发的功能**：明确功能需求，确定模块名称和功能描述
  2. **生成前端页面骨架**：使用 `scripts/generate-sql.sh -group <group> -name <name>` 生成前端页面 xxx.vue 骨架文件
     - **脚本执行规范**：如果需要执行 `scripts/` 目录下的脚本，需要用户执行后再通知进行下一步；如果是其他脚本（如数据库 SQL 脚本），AI 可以直接执行
  3. **等待后端完成**：等待后端完成 API 定义和实现，确保后端接口可独立测试通过
  4. **生成 TS 代码**：使用 `scripts/generate-ts.sh <api_file>` 从后端 .api 文件生成 TS 代码
     - **脚本执行规范**：如果需要执行 `scripts/` 目录下的脚本，需要用户执行后再通知进行下一步；如果是其他脚本（如数据库 SQL 脚本），AI 可以直接执行
  5. **完善前端页面**：基于生成的页面骨架和 TS 代码，完善前端页面和组件功能
  6. **前后端联调**：前后端联调接口，测试完整流程，确保功能正常
  7. **测试通过后再开发下一个功能**：每个功能必须完整实现并测试通过后，才能开始下一个功能的开发
- **通用组件使用规范**：
  - **D2Table 组件**（`src/components/common/D2Table.vue`）：适用于所有涉及表格展示、分页、详情/编辑、新增的业务页面
    - 功能：统一的表格展示、分页、内置详情/编辑抽屉、内置新增抽屉、支持多种列类型
    - 使用场景：列表管理页面（如角色管理、权限管理、用户管理等）
    - 参考示例：`src/views/system/RoleList.vue`、`src/views/system/PermissionList.vue`、`src/views/system/UserList.vue`
    - 组件文档：`src/components/common/README.md`
    - 类型定义：`src/types/table.ts`
  - **el-tree 组件**：适用于树形数据展示（如部门管理、菜单管理）
    - 参考示例：`src/views/system/DepartmentList.vue`、`src/views/system/MenuList.vue`
- **数据字典使用规范（必须遵守）**：
  - **字典的使用场景**：
    - **状态枚举**：用户状态、订单状态、审核状态等需要在多个地方使用的状态值
    - **类型分类**：文件类型、存储类型、支付方式、物流方式等业务类型
    - **选项列表**：性别、是否、地区、行业等需要在表单中下拉选择的选项
    - **业务常量**：需要在多个模块中复用的常量值，如用户等级、会员类型等
    - **配置选项**：需要动态调整且可能频繁变更的配置项，如通知类型、消息模板类型等
  - **优先考虑使用字典的情况**：
    - 前端需要在下拉选择框（`el-select`）、单选框（`el-radio-group`）、多选框（`el-checkbox-group`）等组件中展示的选项
    - 需要在多个页面或模块中复用的枚举值
    - 需要由业务人员或管理员动态维护的选项（不需要改代码）
    - 需要支持多语言或国际化显示的选项
    - 需要在表格列中显示标签（`el-tag`）的状态或类型字段
    - 需要在搜索条件中使用的筛选选项
  - **字典使用流程**：
    1. 在开发新功能时，先评估字段是否需要使用字典
    2. 如果需要，**后端开发人员编写增量字典插入 SQL 语句**（见后端文档「字典 SQL 插入规范」）
    3. 后端执行字典 SQL 语句，创建字典类型和字典项
    4. **前端统一字典管理**：在 `src/stores/dict.ts` 的 `REQUIRED_DICT_CODES` 数组中添加新的字典类型编码
    5. **登录后自动加载**：用户登录后会自动批量加载所有必需的字典数据（通过 `POST /api/v1/dict/batch` 接口）
    6. **在组件中使用字典**：使用 `useDictOptions` composable 获取字典选项，避免硬编码
    7. 在表格列中使用字典项进行值转换显示（如状态标签）
  - **字典 SQL 插入规范（后端负责）**：
    - **字典数据通过 SQL 语句新增**：所有字典类型和字典项的数据都通过 SQL 语句插入，不在管理界面中手动创建
    - **增量 SQL 格式**：在 `admin-server/db/data.sql` 文件中，字典相关的 SQL 应放在统一位置
    - **字典类型 SQL 模板**：
      ```sql
      -- {功能模块}相关字典类型
      INSERT INTO `admin_dict_type` (`id`, `name`, `code`, `description`, `status`, `created_at`, `updated_at`, `deleted_at`)
      VALUES 
        ({id}, '{字典类型名称}', '{字典类型编码}', '{字典类型描述}', 1, UNIX_TIMESTAMP(), UNIX_TIMESTAMP(), 0)
      ON DUPLICATE KEY UPDATE 
        `name`=VALUES(`name`), 
        `description`=VALUES(`description`), 
        `updated_at`=UNIX_TIMESTAMP(), 
        `deleted_at`=0;
      ```
    - **字典项 SQL 模板**：
      ```sql
      -- {字典类型名称}字典项
      INSERT INTO `admin_dict_item` (`id`, `type_id`, `label`, `value`, `sort`, `status`, `remark`, `created_at`, `updated_at`, `deleted_at`)
      VALUES 
        ({id1}, {type_id}, '{字典项标签1}', '{字典项值1}', 1, 1, '{备注1}', UNIX_TIMESTAMP(), UNIX_TIMESTAMP(), 0),
        ({id2}, {type_id}, '{字典项标签2}', '{字典项值2}', 2, 1, '{备注2}', UNIX_TIMESTAMP(), UNIX_TIMESTAMP(), 0)
      ON DUPLICATE KEY UPDATE 
        `label`=VALUES(`label`), 
        `value`=VALUES(`value`), 
        `sort`=VALUES(`sort`), 
        `status`=VALUES(`status`), 
        `remark`=VALUES(`remark`), 
        `updated_at`=UNIX_TIMESTAMP(), 
        `deleted_at`=0;
      ```
    - **前端使用字典的注意事项**：
      - 前端不需要关心字典数据的创建，只需通过 API 获取字典项列表
      - 如果字典数据不存在，API 会返回空列表，前端应做容错处理
      - 字典项的 `value` 字段通常存储为字符串，前端使用时可能需要类型转换
  - **前端统一字典管理（必须遵守）**：
    - **字典 Store**：`src/stores/dict.ts` 统一管理全局字典数据
    - **批量加载**：登录后通过 `POST /api/v1/dict/batch` 接口一次性加载所有必需的字典
    - **必需字典列表**：在 `REQUIRED_DICT_CODES` 数组中维护所有需要加载的字典类型编码
    - **自动加载时机**：用户登录成功后自动调用 `dictStore.loadDicts()` 加载字典
    - **字典数据缓存**：字典数据存储在 Pinia store 中，整个会话期间有效，登出时清除
  - **前端字典使用方式（推荐使用 useDictOptions）**：
    ```typescript
    // ✅ 推荐：使用 useDictOptions composable（响应式，自动从store获取）
    import { useDictOptions } from '@/composables/useDictOptions';
    
    // 在组件中使用
    const { options: statusOptions, getLabel: getStatusLabel } = useDictOptions(
      'user_status',
      [
        { label: '启用', value: '1' },  // 默认值（字典为空时使用）
        { label: '禁用', value: '0' }
      ]
    );
    
    // 在模板中使用
    <el-select v-model="query.status">
      <el-option
        v-for="item in statusOptions"
        :key="item.value"
        :label="item.label"
        :value="item.value"
      />
    </el-select>
    
    // 在 D2Table 列配置中使用
    const drawerColumns = computed<DrawerColumn[]>(() => [
      {
        prop: 'status',
        label: '状态',
        type: D2TableElemType.Select,
        options: statusOptions.value  // 注意：需要 .value 获取实际数组
      }
    ]);
    ```
  - **字典使用禁止事项**：
    - ❌ **禁止硬编码选项**：所有 `el-select`、`el-radio-group`、`el-checkbox-group` 等组件的选项必须从字典获取
    - ❌ **禁止直接调用 dictGet API**：应使用 `useDictOptions` composable 或 `dictStore.getDictOptions()`
    - ❌ **禁止在组件中单独加载字典**：字典应在登录后统一批量加载，不要在组件中单独调用 API
    - ✅ **必须添加到 REQUIRED_DICT_CODES**：新增字典类型后，必须在 `src/stores/dict.ts` 的 `REQUIRED_DICT_CODES` 数组中添加

---

## 2. 核心功能实现

### 2.1 认证与登录
- **登录页面**：`src/views/Login.vue`
- **Token 管理**：`src/stores/user.ts`（localStorage 持久化）
- **请求拦截**：`src/utils/request.ts`（自动添加 Authorization 头）
- **登录流程**：登录 → 获取 Token → 获取用户信息 → 获取菜单 → 加载字典 → 连接 WebSocket

### 2.2 权限管理
- **路由级权限**：`src/router/index.ts`（meta.permission）
- **按钮级权限**：`src/directives/permission.ts`（v-permission 指令）
- **权限判断**：`src/hooks/usePermission.ts`（hasPermission 函数）
- **权限数据**：`src/stores/user.ts`（permissions 数组，支持 `*` 超级权限）

### 2.3 菜单管理
- **菜单数据**：`src/stores/user.ts`（menus 数组，从 `/api/v1/menus/my-tree` 获取）
- **菜单渲染**：`src/components/layout/AppSidebar.vue`
- **动态路由**：`src/router/index.ts`（根据菜单树自动注入路由）
- **菜单缓存**：localStorage 缓存 5 分钟（TTL）

### 2.4 数据字典
- **字典 Store**：`src/stores/dict.ts`（统一管理全局字典数据）
- **批量加载**：登录后通过 `POST /api/v1/dict/batch` 一次性加载所有必需的字典
- **必需字典列表**：在 `REQUIRED_DICT_CODES` 数组中维护
- **字典使用**：`src/composables/useDictOptions.ts`（useDictOptions composable）

### 2.5 通用组件
- **D2Table 组件**：`src/components/common/D2Table.vue`
  - 功能：统一的表格展示、分页、内置详情/编辑抽屉、内置新增抽屉
  - 使用场景：所有列表管理页面（如角色管理、权限管理、用户管理等）
  - 参考示例：`src/views/system/RoleList.vue`、`src/views/system/UserList.vue`
  - 组件文档：`src/components/common/README.md`
  - 类型定义：`src/types/table.ts`

### 2.6 主题与国际化
- **主题切换**：`src/styles/theme.scss`（通过 `data-theme` 属性切换明亮/暗色）
- **国际化**：`src/i18n/index.ts`（支持中文/英文）
- **样式变量**：`src/styles/variables.scss`（断点、颜色等）

### 2.7 移动端适配
- **断点定义**：`src/styles/variables.scss`（`$screen-sm: 768px`、`$screen-md: 1024px` 等）
- **响应式布局**：使用媒体查询适配不同屏幕尺寸
- **表格适配**：小屏幕下表格横向滚动，表单垂直布局
- **参考示例**：`src/views/public/VideoListPage.vue`、`src/views/system/UserList.vue`

### 2.8 WebSocket 实时通信
- **WebSocket Store**：`src/stores/websocket.ts`（统一管理 WebSocket 连接）
- **自动连接**：登录后自动连接，登出时断开
- **使用场景**：聊天功能、实时通知等

---

## 3. 开发规范

### 3.1 代码规范
- **ESLint + Prettier**：统一代码风格
- **TypeScript 严格模式**：类型安全
- **组件命名**：PascalCase（如 `UserList.vue`）
- **文件命名**：kebab-case（如 `use-dict-options.ts`，但 composables 使用 camelCase）

### 3.2 API 调用规范
- **统一从 `@/api/generated/admin` 导入**：使用 goctl 生成的接口函数
- **二次封装**：在 `src/api/*.ts` 中统一错误处理、适配返回结构
- **时间字段规范**：后端返回 `int64` 时间戳（秒级），前端负责格式化显示

### 3.3 字典使用规范
- **禁止硬编码选项**：所有下拉选择、单选框等组件的选项必须从字典获取
- **使用 useDictOptions**：统一使用 `useDictOptions` composable 获取字典选项
- **添加到 REQUIRED_DICT_CODES**：新增字典类型后，必须在 `src/stores/dict.ts` 的 `REQUIRED_DICT_CODES` 数组中添加

### 3.4 组件使用规范
- **D2Table 组件**：适用于所有涉及表格展示、分页、详情/编辑、新增的业务页面
- **el-tree 组件**：适用于树形数据展示（如部门管理、菜单管理）

### 3.5 环境配置

#### 开发环境
- **开发服务器**：`pnpm dev`（Vite dev server，端口 5173）
- **API 代理**：`vite.config.ts` 中配置 `/api` 代理到 `http://localhost:20000`
- **环境变量**：`.env.development`（如需要）

#### 生产环境
- **构建命令**：`pnpm build`（输出到 `dist/`）
- **部署路径**：`/admin/`（在 `vite.config.ts` 中配置 `base: '/admin/'`）
- **API 请求**：通过 Nginx 代理 `/gateway/api/*` 到后端

---

## 4. 已完成功能
- 初始化 admin-frontend（Vite + Vue3 + TS + Pinia + Vue Router + Element Plus），内置登录页、基础布局、菜单导航。
- 封装 Axios 请求与 Token 拦截（`src/utils/request.ts`），Pinia 用户状态与登录/登出（`src/stores/user.ts`）。
- 角色管理页面：列表查询、分页、新增、编辑、删除，对接 `/api/v1/roles`。
- 权限管理页面：列表查询、分页、新增、编辑、删除，对接 `/api/v1/permissions`。
- 部门管理页面：部门树展示、新增/编辑/删除，对接 `/api/v1/departments/tree` 等接口。
- 菜单管理页面：菜单树展示、新增/编辑/删除，对接 `/api/v1/menus/tree` 等接口。
- 用户管理页面：列表查询、分页、新增、编辑、删除，对接 `/api/v1/users`。
- goctl TS 生成脚本：`pnpm api:gen`（调用 `scripts/api-gen.mjs` 批量生成后端 .api 的 TS 代码）。
- 按钮级权限指令 `v-permission`，路由级权限校验（meta.permission），菜单/按钮按权限过滤渲染。
- 菜单树拉取 `/api/v1/menus/tree` 并在侧栏展示。
- 主题样式基础变量、403/404 错误页，内置 i18n（zh/en 文案覆盖），主题切换（明亮/暗色）。
- 权限/菜单本地缓存（TTL 5min），登录后自动加载缓存并按需刷新。
- 动态路由注入与 KeepAlive 缓存（菜单路径映射至页面组件，meta.keepAlive 为 true）。
- 阶段四 系统支撑页面：
  - 系统配置管理页面（ConfigList.vue）：列表查询、分页、新增、编辑、删除，支持刷新缓存功能。
  - 数据字典类型管理页面（DictTypeList.vue）：列表查询、分页、新增、编辑、删除，支持刷新缓存功能。
  - 数据字典项管理页面（DictItemList.vue）：列表查询、分页、新增、编辑、删除。
  - 文件管理页面（FileList.vue）：列表查询、分页、新增、编辑、删除，支持文件上传和下载功能。
- 通用组件集成：所有列表管理页面已集成 `D2Table` 组件，统一表格展示、分页、详情/编辑、新增功能。
- demo 功能页面（开发流程示例）：
  - 演示功能管理页面（DemoList.vue）：列表查询、分页、新增、编辑、删除，使用 `D2Table` 组件
  - 权限绑定：`create-permission="demo:create"`、`update-permission="demo:update"`、`delete-permission="demo:delete"`
  - 前端页面由 `sqlgen.exe` 自动生成，包含完整的 CRUD 功能和权限控制
- 阶段七 聊天功能页面：
  - 聊天会话列表页面（ChatList.vue）：展示当前用户的私聊和群组会话列表，支持实时消息接收。
  - 聊天消息列表页面（ChatMessageList.vue）：展示聊天消息列表，支持发送消息、实时消息接收、消息删除。
  - 聊天群组列表页面（ChatGroupList.vue）：群组列表查询、分页、新增、编辑、删除，群组成员管理。
  - WebSocket 集成：实现 WebSocket 客户端连接，支持实时消息推送、在线状态更新。
- 阶段八 公告与通知页面：
  - 公告管理页面（NoticeList.vue）：列表查询、分页、新增、编辑、删除，支持按类型、状态筛选，定时发布功能。
  - 消息通知页面（NotificationList.vue）：通知列表查询、分页，支持标记已读、全部已读、清除已读、删除通知，实时通知提醒。
- 阶段九 性能日志页面：
  - 性能日志列表页面（PerformanceLogList.vue）：性能日志查询、分页，支持按方法、路径、慢接口、状态码、时间范围筛选，用于性能分析和问题排查。
- 阶段十 视频管理页面：
  - 视频列表管理页面（VideoList.vue）：列表查询、分页、新增、编辑、删除，支持关键词搜索，展示视频封面、名称、时长、播放链接、创建时间，支持播放按钮跳转到播放器。
  - 视频播放器页面（VideoPlayer.vue）：支持手动输入视频链接播放，支持从列表跳转播放，使用video.js播放器，支持m3u8、mp4等多种格式，智能代理播放（先直接播放，失败后使用代理）。
- 阶段十一 异步任务模块页面：
  - 任务管理页面（TaskList.vue）：任务列表查询、分页、筛选（任务名称、类型、执行类型、状态、创建用户、创建时间范围），任务详情抽屉（展示任务基础信息、参数JSON、结果JSON、错误信息、下载按钮），任务取消功能。
  - 浮动任务球组件（TaskFloatBall.vue）：右下角固定浮动球，展示最近任务列表（任务名称、状态、创建时间），点击任务跳转到任务管理页面，已完成导出任务显示下载按钮，实时更新任务状态（通过WebSocket）。
  - 导出功能改造：操作日志、登录日志、审计日志、SDK调用日志、性能日志的导出按钮统一改为调用导出接口创建异步任务，不再直接处理blob下载。
- SDK 管理页面（首期）：
  - SDK API Key 管理页面（基于 `temp/SdkList.vue`）：对接 `/api/v1/sdk/key/list/create/update/delete`，使用 `D2Table` 展示 id、名称、apiKey、apiSecret、状态、过期时间、创建时间等字段，支持分页查询、新增、编辑、删除。
  - 表单/抽屉：支持配置状态、过期时间（秒级时间戳）、IP 白名单（多行文本）、备注等字段，权限绑定使用 `sdk_key:create/update/delete`。
  - SDK 文件上传 TS 调用已生成（`sdkFileUpload`），可用于后续 SDK 示例/工具集成。
 - 数据统计页面：
   - 页面：`src/views/system/MetricStats.vue`；
   - 接口：`GET /api/v1/metrics/stats`，通过 `metricApi.stats` 调用；
   - 功能：按 module（如 `blog_article_detail`）、业务 ID（文章 ID/视频 ID，列表页为 0）、统计日期查询 PV/UV/VV/IP 聚合结果，数据来源为后端 MySQL 表 `metric_daily_stats`；
   - 视觉：在数值卡片基础上增加 PV/UV/VV/IP 柱状对比图，便于快速观察各指标相对大小。

---

## 5. 待实现 / 待完善功能
- [✅] 阶段三：RBAC 权限/组织/菜单管理（用户/角色/权限/部门/菜单/按钮）
  - [✅] 角色管理页面（列表、新增、编辑、删除）
  - [✅] 权限管理页面（列表、新增、编辑、删除）
  - [✅] 部门管理页面（树形展示、新增、编辑、删除）
  - [✅] 菜单管理页面（树形展示、新增、编辑、删除）
  - [✅] 用户管理页面（列表、新增、编辑、删除）
- [✅] 阶段四：系统支撑（系统配置、数据字典、文件存储）
  - [✅] 系统配置管理页面（列表、新增、编辑、删除、刷新缓存）
  - [✅] 数据字典类型管理页面（列表、新增、编辑、删除、刷新缓存）
  - [✅] 数据字典项管理页面（列表、新增、编辑、删除）
  - [✅] 文件管理页面（列表、新增、编辑、删除、上传、下载）
- [✅] 阶段七：聊天功能（聊天会话、群组管理、实时通信）
  - [✅] 聊天会话列表页面（ChatList.vue）
  - [✅] 聊天消息列表页面（ChatMessageList.vue）
  - [✅] 聊天群组列表页面（ChatGroupList.vue）
  - [✅] WebSocket 实时通信集成
- [✅] 阶段八：公告与通知
  - [✅] 公告管理页面（NoticeList.vue）
  - [✅] 消息通知页面（NotificationList.vue）
- [✅] 阶段九：性能日志
  - [✅] 性能日志列表页面（PerformanceLogList.vue）
- [✅] 阶段十一：异步任务模块
  - [✅] 任务管理页面（TaskList.vue）
  - [✅] 浮动任务球组件（TaskFloatBall.vue）
  - [✅] 导出功能改造（操作日志、登录日志、审计日志、SDK调用日志、性能日志）
- [ ] 菜单组件路径与后端约定的自动映射策略（当前手工 map）。
- [ ] 主题更多维度（色板、字体），国际化文案进一步细化。
- [ ] 单元测试与 E2E 验证。
 - [ ] SDK 管理后续页面：
   - [ ] SDK 接口管理页面：复用 D2Table 实现 `/api/v1/sdk/interface/list/create/update/delete` 的前端管理，支持配置默认限频、状态等。
   - [ ] SDK 调用记录页面：列表查询 `/api/v1/sdk/call/log/list`，支持按 key、apiCode、IP、状态码、时间筛选并触发导出。

---

## 6. 技术决策记录
- 2025-12-24：前端使用 goctl TS 生成 + Axios 二次封装；对生成代码中的 path param 问题在自定义 API 层修正（不修改 generated）。
- 2025-12-24：权限指令 v-permission 基于 Pinia 权限列表与 `permissions.includes('*')`；路由 meta.permission 校验不通过跳转 403。
- 2025-12-24：权限/菜单本地缓存 TTL 5 分钟；动态路由由菜单树驱动并开启 KeepAlive；主题切换通过 `data-theme`。
- 2026-01-13：**导出表格统一走异步任务 + 文件管理**：所有列表导出按钮（操作日志、登录日志、审计日志、SDK 调用日志、性能日志）只负责调用后端导出接口以创建异步任务，不再直接请求 `blob` 并触发浏览器下载；下载文件的行为统一为：
  - 导出接口调用成功后，通过右下角浮动任务球或「任务管理」页面查看任务状态；
  - 任务完成后，从任务详情中的结果 JSON（如 `fileUrl`）获取下载链接，浮动任务球中已完成导出任务直接显示下载按钮；
  - 由文件管理模块暴露的 URL 统一承载下载逻辑，前端不直接拼接物理路径，也不绕开文件管理模块。

---

## 7. 关键代码位置
- 入口：`src/main.ts`，路由：`src/router/index.ts`，布局：`src/layouts/DefaultLayout.vue`，登录：`src/views/Login.vue`。
- 状态：`src/stores/user.ts`；请求封装：`src/utils/request.ts`。
- 角色页：`src/views/system/RoleList.vue`；API 封装：`src/api/role.ts`。
- 权限页：`src/views/system/PermissionList.vue`；API 封装：`src/api/permission.ts`。
- 部门页：`src/views/system/DepartmentList.vue`；API 封装：`src/api/department.ts`。
- 菜单页：`src/views/system/MenuList.vue`；API 封装：`src/api/menu.ts`。
- 用户页：`src/views/system/UserList.vue`；API 封装：`src/api/user.ts`。
- 系统配置页：`src/views/system/ConfigList.vue`；API 封装：`src/api/generated/admin.ts`。
- 字典类型页：`src/views/system/DictTypeList.vue`；API 封装：`src/api/generated/admin.ts`。
- 字典项页：`src/views/system/DictItemList.vue`；API 封装：`src/api/generated/admin.ts`。
- 文件管理页：`src/views/system/FileList.vue`；API 封装：`src/api/generated/admin.ts`。
- 聊天功能页：`src/views/chatroom/ChatList.vue`、`src/views/chatroom/ChatMessageList.vue`、`src/views/chatroom/ChatGroupList.vue`；API 封装：`src/api/generated/admin.ts`。
- 公告管理页：`src/views/system/NoticeList.vue`；API 封装：`src/api/generated/admin.ts`。
- 消息通知页：`src/views/system/NotificationList.vue`；API 封装：`src/api/generated/admin.ts`。
- 性能日志页：`src/views/system/PerformanceLogList.vue`；API 封装：`src/api/generated/admin.ts`。
- 视频管理页：`src/views/video/VideoList.vue`；API 封装：`src/api/generated/admin.ts`。
- 视频播放器页：`src/views/video/VideoPlayer.vue`；使用video.js播放器库。
- 任务管理页：`src/views/system/TaskList.vue`；API 封装：`src/api/generated/admin.ts`（taskList、taskDetail、taskCancel、taskRecent）。
- 浮动任务球组件：`src/components/common/TaskFloatBall.vue`；WebSocket 状态：`src/stores/websocket.ts`（recentTasks、refreshRecentTasks、handleTaskNotification、handleTaskProgress）。
- goctl 生成目录：`src/api/generated/`；生成脚本：`scripts/generate-ts.sh`。
- 权限指令与钩子：`src/directives/permission.ts`，`src/hooks/usePermission.ts`。
- 菜单 API 与渲染：`src/api/menu.ts`，`src/layouts/DefaultLayout.vue`。
- 错误页：`src/views/error/NoAccess.vue`，`src/views/error/NotFound.vue`。
- 主题与 i18n：`src/styles/theme.scss`，`src/i18n/index.ts`。
- 路由/缓存：`src/router/index.ts`（动态注入 + meta.keepAlive）、`src/App.vue`（KeepAlive 包裹）。

---

## 8. 接口对接进度
- 角色管理：GET/POST/PUT/DELETE `/api/v1/roles` ✅
- 权限管理：GET/POST/PUT/DELETE `/api/v1/permissions` ✅
- 部门管理：GET `/api/v1/departments/tree`，POST/PUT/DELETE `/api/v1/departments/:id` ✅
- 菜单管理：GET `/api/v1/menus/tree`，POST/PUT/DELETE `/api/v1/menus/:id` ✅
- 用户管理：GET/POST/PUT/DELETE `/api/v1/users` ✅
- 认证：POST `/api/v1/auth/login`、POST `/api/v1/auth/logout`、GET `/api/v1/auth/profile` ✅（登录页接入）
- 系统配置管理：GET `/api/v1/configs`、GET `/api/v1/configs/:key`、POST/PUT/DELETE `/api/v1/configs` ✅
- 数据字典类型管理：GET/POST/PUT/DELETE `/api/v1/dict-types` ✅
- 数据字典项管理：GET/POST/PUT/DELETE `/api/v1/dict-items` ✅
- 公共字典查询：GET `/api/v1/dict` ✅
- 文件管理：GET/POST/PUT/DELETE `/api/v1/files`、POST `/api/v1/files/upload`、GET `/api/v1/files/:id/download` ✅
- 缓存管理：POST `/api/v1/cache/refresh` ✅
- demo 管理：GET/POST/PUT/DELETE `/api/v1/demos` ✅（开发流程示例）
- 聊天功能：
  - GET `/api/v1/chats` ✅（聊天会话列表）
  - POST `/api/v1/chats/messages` ✅（发送消息）
  - GET `/api/v1/chats/messages/list` ✅（消息列表）
  - GET/POST/PUT/DELETE `/api/v1/chats/groups` ✅（群组管理）
  - GET `/api/v1/chats/groups/:id` ✅（群组详情）
  - GET `/api/v1/chats/groups/:id/members` ✅（成员列表）
  - POST `/api/v1/chats/groups/members` ✅（添加成员）
  - DELETE `/api/v1/chats/groups/members` ✅（移除成员）
  - GET `/api/v1/chats/messages` ✅（管理员消息列表）
  - DELETE `/api/v1/chats/messages` ✅（删除消息）
  - WebSocket `/api/v1/chats/ws` ✅（实时通信）
- 公告管理：GET/POST/PUT/DELETE `/api/v1/notices` ✅
- 消息通知：
  - GET `/api/v1/notifications` ✅（通知列表）
  - PUT `/api/v1/notifications/read` ✅（标记已读）
  - PUT `/api/v1/notifications/read-all` ✅（全部已读）
  - DELETE `/api/v1/notifications/read` ✅（清除已读）
  - DELETE `/api/v1/notifications` ✅（删除通知）
- 性能日志：GET `/api/v1/performance-logs` ✅
- 任务管理：
  - GET `/api/v1/tasks` ✅（任务列表）
  - GET `/api/v1/tasks/detail` ✅（任务详情）
  - POST `/api/v1/tasks/cancel` ✅（取消任务）
  - GET `/api/v1/tasks/recent` ✅（最近任务，登录即可访问）
- 导出功能（异步任务）：
  - GET `/api/v1/operation-logs/export` ✅（异步任务）
  - GET `/api/v1/login-logs/export` ✅（异步任务）
  - GET `/api/v1/audit-logs/export` ✅（异步任务）
  - GET `/api/v1/sdk/call/log/export` ✅（异步任务）
  - GET `/api/v1/performance-logs/export` ✅（异步任务）
- 视频管理：
  - GET `/api/v1/videos` ✅（视频列表）
  - POST `/api/v1/videos` ✅（新增视频）
  - PUT `/api/v1/videos` ✅（更新视频）
  - DELETE `/api/v1/videos` ✅（删除视频）
  - GET `/api/v1/videos/proxy` ✅（视频代理，返回视频流）
 - 通用打点与统计：
   - POST `/api/v1/metrics/report` ✅（通用 PV/UV/VV/IP 上报，封装为 `metricApi.report` 并在公共博客/视频页面统一调用）；
   - GET `/api/v1/metrics/stats` ✅（日级 PV/UV/VV/IP 统计查询，封装为 `metricApi.stats` 并在 `MetricStats.vue` 中使用）。

---

## 9. 博客功能模块前端实现方案（已评审 & 已实现）

### 9.1 页面规划

- 后台管理：
  - `views/blog/BlogTagList.vue`：博客标签管理（列表 + 新增/编辑/删除）。
  - `views/blog/BlogArticleList.vue`：博客文章管理（列表、创建/编辑、提交审核、上架/下架）。
  - `views/blog/BlogArticleAuditList.vue`：文章审核列表（待审核/审核历史，支持通过/驳回/审核下架）。
- 公共页面：
  - `views/public/BlogListPage.vue`：公共博客文章列表页（支持搜索、分页、摘要展示）。
  - `views/public/BlogDetailPage.vue`：公共博客文章详情页（Markdown 文本展示，后续可替换为渲染组件）。

### 9.2 API 封装

- 生成的 TS 接口：`src/api/generated/admin.ts` / `adminComponents.ts` 中的：
  - 标签管理：`blogTagList/blogTagCreate/blogTagUpdate/blogTagDelete`。
  - 文章管理：`blogArticleList/blogArticleCreate/blogArticleUpdate/blogArticleDelete/blogArticleDetail`。
  - 状态流转：`blogArticleSubmit/blogArticlePublish/blogArticleUnpublish`。
  - 审核操作：`blogArticleAudit/blogArticleAuditUnpublish`。
  - 公共文章：`publicBlogArticleList/publicBlogArticleDetail`。
  - 打点上报：`metricReport`（`MetricReportReq`）。
- 二次封装：
  - `src/api/blog.ts`：对博客相关 API 进行集中封装（`blogApi`），统一出入参类型。
  - `src/api/metric.ts`：对 `/api/v1/metrics/report` 封装为 `metricApi.report`，供文章/视频等公共页面共用。

### 9.3 字典与配置

- 在 `src/stores/dict.ts` 的 `REQUIRED_DICT_CODES` 中新增：
  - `blog_article_status`：博客文章业务状态（列表筛选 + 状态标签显示）。
  - `blog_article_audit_status`：博客文章审核状态（列表筛选 + 审核状态标签显示）。
- 组件使用：
  - 后台文章列表页通过 `useDictOptions('blog_article_status')`、`useDictOptions('blog_article_audit_status')` 获取选项与标签转换。

### 9.4 后台页面实现（D2Table）

- `BlogTagList.vue`：
  - 使用 `D2Table` 实现标签列表、分页、详情/编辑抽屉、新增抽屉。
  - 支持按名称与状态筛选，状态使用数值（1=启用，2=禁用），与后端约定一致。
  - 权限控制：`create-permission="blog_tag:create"`、`update-permission="blog_tag:update"`、`delete-permission="blog_tag:delete"`。
- `BlogArticleList.vue`：
  - 使用 `D2Table` 实现文章列表（标题、作者、状态、审核状态、标签、上架时间等）。
  - 筛选条件：标题关键字、状态（字典 `blog_article_status`）、审核状态（字典 `blog_article_audit_status`）。
  - 支持在列表操作列中执行提交审核、上架、下架操作，前端根据当前状态/审核状态控制按钮显隐。
  - **交互调整**：文章新增/编辑不在抽屉内完成，而是跳转到独立编辑页 `BlogArticleEdit.vue`（Markdown 编辑体验更好）。
- `BlogArticleAuditList.vue`：
  - 专门给审核员使用的审核列表页，默认筛选 `auditStatus=待审核`。
  - 操作：
    - 「审核通过」/「驳回」：调用 `blogArticleAudit`，支持输入审核意见。
    - 「审核下架」：调用 `blogArticleAuditUnpublish`，支持输入下架原因。

### 9.5 公共页面与打点

- `BlogListPage.vue`：
  - 使用卡片列表展示公共文章（封面 / 标题 / 作者 / 发布时间 / 标签 / 摘要），摘要长度由后端按字典截断。
  - 支持关键词搜索与分页。
  - 每次加载列表完成后，通过 `metricApi.report({ module: 'blog_article_list', bizId: 0, event: 'view' })` 上报 PV/UV/VV/IP。
- `BlogDetailPage.vue`：
  - 展示单篇公共文章详情（标题、作者、发布时间、标签、封面、Markdown 原文文本）。
  - 当前阶段先以 `<pre>` 直接展示 Markdown 文本，后续可替换为 Markdown 渲染组件。
  - 加载成功后，上报 `metricApi.report({ module: 'blog_article_detail', bizId: id, event: 'view' })`。

### 9.6 路由与访问控制

- 公共路由（无需登录）已在 `src/router/index.ts` 中注册：
  - `/public/blog` → `PublicBlogList` → `BlogListPage.vue`。
  - `/public/blog/:id` → `PublicBlogDetail` → `BlogDetailPage.vue`。
  - 在路由守卫中，将 `/public/blog` 加入公共路径白名单，跳过登录校验。
- 后台路由（需登录）：
  - 通过菜单 `component` 字段与 `buildRoutesFromMenus` 动态注入：
    - `/blog/tag` → `views/blog/BlogTagList.vue`。
    - `/blog/article` → `views/blog/BlogArticleList.vue`。
    - `/blog/article-audit` → `views/blog/BlogArticleAuditList.vue`。
  - **补充：非菜单路由（从列表跳转进入）**
    - `/blog/article/edit` → `views/blog/BlogArticleEdit.vue`（新增文章）。
    - `/blog/article/edit/:id` → `views/blog/BlogArticleEdit.vue`（编辑文章）。
    - 该路由由前端静态注册，权限分别使用 `blog_article:create` / `blog_article:update`。

### 9.7 博客前端开发进度

- [✅] 9.7.1 生成博客模块 TS 接口（`./scripts/generate-ts.sh api/admin.api`）。
- [✅] 9.7.2 接入博客相关字典（`blog_article_status`、`blog_article_audit_status`）到 `REQUIRED_DICT_CODES`。
- [✅] 9.7.3 封装博客与打点 API（`src/api/blog.ts`、`src/api/metric.ts`）。
- [✅] 9.7.4 实现后台标签管理页面 `BlogTagList.vue` 并接入权限控制。
- [✅] 9.7.5 实现后台文章管理页面 `BlogArticleList.vue`（含提交审核 / 上下架操作）。
- [✅] 9.7.6 实现文章审核页面 `BlogArticleAuditList.vue`（审核通过 / 驳回 / 审核下架）。
- [✅] 9.7.7 实现公共博客列表/详情页面（`BlogListPage.vue`、`BlogDetailPage.vue`）并接入 `/metrics/report`。
- [✅] 9.7.8 交互优化：标签新增仅填写名称；文章新增/编辑跳转独立 Markdown 编辑页；封面使用上传组件；正文图片上传支持插入 Markdown。
- [✅] 9.7.9 完善文章编辑流程：
  - 编辑权限控制：上架状态（`status=4`）的文章不可编辑，前端显示只读模式和提示信息。
  - 状态流转：根据原状态自动设置编辑后的状态（草稿保持草稿，其他状态转为待审核）。
  - 按钮显示：根据文章状态动态显示保存按钮文案（"保存草稿" 或 "保存并重新提交审核"）。
  - 关键实现：`admin-frontend/src/views/blog/BlogArticleEdit.vue`，通过 `canEdit`、`saveButtonText`、`canSubmitAudit` 等计算属性控制 UI 交互。
- [✅] 9.7.10 公共页面优化：
  - 修复封面图压缩问题：详情页使用 `object-fit: contain` 和 `max-height`，列表页使用 `aspect-ratio: 16/9`。
  - 集成 ICP 备案号组件：公共博客列表页和详情页底部添加 `IcpFooter` 组件（后续已扩展到所有 public 列表/详情页）。
  - 优化公共列表页 UI：添加 Hero 区域、卡片网格布局与柔和暖色背景，整体对齐「小程序风格」。
  - 关键实现：`admin-frontend/src/views/public/BlogDetailPage.vue`、`admin-frontend/src/views/public/BlogListPage.vue`。
- [✅] 9.7.11 实现数据统计页面：
  - 创建 `MetricStats.vue`：支持按 module/bizId/day 查询 PV/UV/VV/IP 统计数据。
  - 更新 `metric.ts` API 封装：添加 `stats` 方法调用 `GET /api/v1/metrics/stats`。
  - 添加路由：`/system/metric-stats`，权限 `metric:stats`。
  - 关键实现：`admin-frontend/src/views/system/MetricStats.vue`，使用卡片式布局 + 柱状图展示统计数据。
- [✅] 9.7.12 通用打点组件与公共页面接入：
  - 新增无渲染组件 `MetricReporter.vue`（`src/components/common/MetricReporter.vue`），通过 `module`、`bizId`、`event` 三个 props 统一封装 `/metrics/report` 上报逻辑，挂载/`bizId` 变更时自动上报，失败静默处理。
  - 公共博客页面：在 `BlogListPage.vue` 中通过 `<MetricReporter module="blog_article_list" :biz-id="0" />` 上报列表 PV/UV/VV/IP，在 `BlogDetailPage.vue` 中通过 `<MetricReporter module="blog_article_detail" :biz-id="Number(route.params.id) || 0" />` 上报详情访问，并统一在页脚渲染 `IcpFooter`。
  - 公共视频页面：在 `VideoListPage.vue`、`VideoDetailPage.vue` 中分别接入 `MetricReporter` 上报 `video_list`、`video_detail` 访问，在视频详情页中额外基于播放器事件单次上报 `play` 事件，同时在所有 public 视频页面底部统一挂载 `IcpFooter`。
- [ ] 9.7.13 前端单元测试与移动端细节优化（如 Markdown 渲染组件、响应式布局进一步优化）：
  - 单元测试（vue-test-utils + vitest，后续补充）：
    - 针对 `BlogArticleEdit.vue` 编写状态流转与按钮文案的测试用例（草稿/待审核/已上架等场景）。
    - 针对 `MetricReporter.vue` 编写上报触发测试（挂载/`bizId` 变化各触发一次上报，异常静默）。
    - 针对 `MetricStats.vue` 编写图表数据计算测试（不同 PV/UV/VV/IP 组合下的百分比高度计算）。
  - 移动端与细节优化：
    - 公共博客/视频列表与详情页增加小屏断点下的栅格调整与排版优化（卡片改为单列、标题字号/间距缩放、按钮易点触）：
      - `BlogListPage.vue`：在 `<768px` 时将博客卡片改为单列栅格，收紧外边距，减小 Hero 区块和卡片的 padding，调整标题与摘要字号；
        - 新增移动端滚动恢复：列表页在进入详情前将分页参数与 `scrollTop` 存入 `sessionStorage`，返回时恢复查询条件与滚动位置；
      - `BlogDetailPage.vue`：在 `<768px` 时缩小容器 padding，降低标题字号，封面高度收缩，并通过 `router.back + sessionStorage` 优先恢复来自列表的分页/查询状态；
      - `VideoListPage.vue`：已实现单列卡片布局、分页组件在窄屏下的收缩与折行展示，以及基于路由 query + `sessionStorage` 的滚动位置恢复；
      - `VideoDetailPage.vue`：在 `<768px` 时收紧播放器下方信息区与磁力链接区的 padding，调整标题/文案字号，保证手机上内容密度适中且易于点击。
    - Markdown 渲染区域在窄屏下增加合适的左右内边距与行高，避免文本贴边、可读性差：
      - `BlogDetailPage.vue` 中为 `.content` 与 `:deep(.md-editor-preview)` 在手机端设置更大的行高与左右留白，同时确保图片 `max-width: 100%` 与圆角样式在小屏下保持一致。
    - 针对滚动返回体验（从详情返回列表）进一步打磨，在路由导航中恢复公共列表的滚动位置与查询参数：
      - 博客/视频列表页在跳转详情前统一将分页参数、搜索关键字与当前滚动位置写入 `sessionStorage`，详情页返回时优先使用浏览器 `history.back()`，否则基于存储状态构造带 query 的列表路由并在列表加载完成后恢复 `scrollTop`。

## 10. Public 界面开发规范（小程序风格）

### 10.1 规范概述

Public 界面（`src/views/public/*`）采用统一的「小程序风格」设计规范，确保所有公共页面（博客、视频等）具有一致的视觉体验和交互模式。

**核心设计原则：**
- **统一背景**：整页使用柔和暖色渐变背景 `linear-gradient(135deg, #fff7e6 0%, #ffe9d9 45%, #ffd1a4 100%)`，内容区域为白色卡片
- **扁平化设计**：轻微阴影（`box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05)`），避免过度装饰
- **响应式布局**：PC 端多列网格，移动端（`≤768px`）单列信息流
- **统一分页**：移动端简化分页组件，只保留「上一页 / 页码 / 下一页」

### 10.2 通用样式模板

#### 10.2.1 列表页模板（`public-list.scss`）

**文件位置：** `admin-frontend/src/styles/public-list.scss`

**使用方式：**
```vue
<template>
  <div class="your-list-page public-list-page">
    <div class="container">
      <!-- Hero 区域（可选） -->
      <div class="hero">
        <div class="hero-title">页面标题</div>
        <div class="hero-subtitle">副标题</div>
        <div class="search-bar">
          <!-- 搜索框 -->
        </div>
      </div>

      <!-- 列表网格 -->
      <div class="list-grid">
        <div v-for="item in list" :key="item.id" class="list-card">
          <div class="cover">
            <!-- 封面图 -->
          </div>
          <div class="card-content">
            <div class="card-title">标题</div>
            <div class="card-meta">元信息</div>
            <div class="card-summary">摘要</div>
            <div class="card-tags">标签</div>
          </div>
        </div>
      </div>

      <!-- 分页 -->
      <div class="pagination">
        <el-pagination ... />
      </div>
    </div>
  </div>
</template>

<style scoped lang="scss">
@import '@/styles/public-list.scss';

// 页面特定样式（如需覆盖通用样式）
.your-list-page {
  // ...
}
</style>
```

**关键类名说明：**
- `.public-list-page`：根容器，提供基础布局（暖色渐变背景、padding、容器宽度，采用 `height: 100vh + overflow: hidden` 的三段式布局，只在中间列表区域滚动）
- `.hero`：顶部标题/搜索区域（可选），白色卡片样式
- `.list-grid`：列表网格容器，PC 端多列，移动端单列
- `.list-card`：列表卡片，移动端自动切换为左图右文布局
- `.cover`：封面图区域，移动端固定宽度 `110px`，高度 `84px`
- `.card-content`：卡片内容区域
- `.card-title`：卡片标题（2 行截断）
- `.card-meta`：元信息（作者、时间等）
- `.card-summary`：摘要（PC 端 2 行，移动端 3 行截断）
- `.card-tags`：标签区域
- `.pagination`：分页组件容器，移动端自动简化

#### 10.2.2 详情页模板（`public-detail.scss`）

**文件位置：** `admin-frontend/src/styles/public-detail.scss`

**使用方式：**
```vue
<template>
  <div class="your-detail-page public-detail-page">
    <div class="container">
      <div class="back-link" @click="goBack">← 返回列表</div>
      <div class="title">文章标题</div>
      <div class="meta">
        <span>作者</span>
        <span class="dot">·</span>
        <span>时间</span>
        <span class="tags">标签</span>
      </div>
      <div class="cover">
        <!-- 封面图 -->
      </div>
      <div class="content">
        <!-- 正文内容 -->
      </div>
    </div>
  </div>
</template>

<style scoped lang="scss">
@import '@/styles/public-detail.scss';

// 页面特定样式（如需覆盖通用样式）
.your-detail-page {
  // ...
}
</style>
```

**关键类名说明：**
- `.public-detail-page`：根容器，提供基础布局（背景、padding、容器宽度）
- `.container`：内容容器，白色卡片，最大宽度 `800px`
- `.back-link`：返回链接
- `.title`：文章标题（PC 端 24px，移动端 21px）
- `.meta`：元信息区域（作者、时间、标签）
- `.cover`：封面图（PC 端最大高度 500px，移动端 240px）
- `.content`：正文内容区域，已优化 Markdown 渲染样式

### 10.3 已应用页面

- ✅ **博客列表页**：`BlogListPage.vue`（使用 `public-list.scss`）
- ✅ **博客详情页**：`BlogDetailPage.vue`（使用 `public-detail.scss`）
- ✅ **视频列表页**：`VideoListPage.vue`（使用 `public-list.scss`，保留视频预览特定样式）
- ✅ **视频详情页**：`VideoDetailPage.vue`（使用 `public-detail.scss`，保留播放器特定样式）

### 10.4 开发流程

**新增 Public 列表+详情页面时：**

1. **创建列表页组件**：
   - 在 `src/views/public/` 下创建 `YourListPage.vue`
   - 根元素添加 `class="your-list-page public-list-page"`
   - 按照模板结构组织 HTML（`.hero`、`.list-grid`、`.list-card` 等）
   - 样式部分引入 `@import '@/styles/public-list.scss'`
   - 如需覆盖通用样式，在 `.your-list-page` 下添加特定样式

2. **创建详情页组件**：
   - 在 `src/views/public/` 下创建 `YourDetailPage.vue`
   - 根元素添加 `class="your-detail-page public-detail-page"`
   - 按照模板结构组织 HTML（`.back-link`、`.title`、`.meta`、`.cover`、`.content`）
   - 样式部分引入 `@import '@/styles/public-detail.scss'`
   - 如需覆盖通用样式，在 `.your-detail-page` 下添加特定样式

3. **滚动位置恢复（列表页）**：
   - 在跳转详情前，将分页参数、搜索关键字、滚动位置存入 `sessionStorage`
   - 返回列表时，优先使用 `router.back()`，否则从 `sessionStorage` 恢复状态
   - 参考实现：`BlogListPage.vue` 中的 `SCROLL_STATE_KEY` 和 `goToDetail` 方法

4. **路由配置**：
   - 在 `src/router/index.ts` 中添加路由（如 `/public/your-module`、`/public/your-module/:id`）
   - 公共页面无需权限控制，路由配置中不添加 `meta.permission`

5. **数据上报**：
   - 列表页：`<MetricReporter module="your_module_list" :biz-id="0" />`
   - 详情页：`<MetricReporter module="your_module_detail" :biz-id="Number(route.params.id) || 0" />`

### 10.5 样式定制

如需在通用模板基础上定制样式，可在组件内覆盖：

```scss
<style scoped lang="scss">
@import '@/styles/public-list.scss';

.your-list-page {
  // 覆盖通用样式
  .list-card {
    // 特定卡片样式
  }

  // 添加特定样式
  .custom-element {
    // ...
  }
}
</style>
```

**注意事项：**
- 优先使用通用模板提供的类名，避免重复定义
- 移动端断点统一使用 `@media (max-width: 768px)`
- 保持与现有页面（博客、视频）的视觉一致性

## 11. 视频与移动端改造方案（规划）
- 已在 `docs/视频与移动端改造方案.md` 中整理统一方案，覆盖：
  - 后端 `ApiEnabledMiddleware` 接入规则与公共字典 `video_proxy_url` 使用方式；
  - 公共视频页（`VideoListPage.vue` / `VideoDetailPage.vue`）的移动端布局优化、分页状态与滚动锚点恢复方案，以及封面预览/标题详情的交互拆分；
  - 后台 `VideoList.vue` / `UserList.vue` 的移动端适配与「名称截断 + 悬停展示完整内容」模式；
  - 全局移动端与交互规范（断点、表格/表单布局、列表返回体验），后续将以视频/用户模块为试点逐步推广到 `src/views` 全部页面。

---

## 12. 博客模块扩展功能实现方案（友情链接、社交信息、文章置顶、标签筛选）

### 12.1 需求概述

**功能模块：**
1. **管理后台-友情链接管理**：增删改查功能，使用 D2Table 组件
2. **管理后台-社交信息管理**：增删改查功能，使用 D2Table 组件
3. **公共博客列表页优化**：`admin-frontend/src/views/public/BlogListPage.vue`
   - 展示友情链接（侧边栏或底部区域）
   - 展示社交信息（侧边栏或底部区域）
   - 支持标签筛选文章（标签选择器）
   - 显示文章置顶标识（置顶文章有特殊标识，如置顶标签、置顶图标等）

**功能点说明：**
- 友情链接和社交信息管理页面放在博客目录下（路由路径：`/blog/friend-link`、`/blog/social-info`）
- 字段长度限制与后端保持一致（名称15个中文字符、链接255字符、备注127个中文字符）
- 文章置顶标识：在列表卡片上显示「置顶」标签或图标，置顶文章排在列表最前面
- 标签筛选：支持多选标签，筛选后更新文章列表

### 12.2 页面设计

#### 12.2.1 管理后台-友情链接管理（`src/views/blog/BlogFriendLinkList.vue`）

**功能：**
- 使用 D2Table 组件实现列表、新增、编辑、删除
- 列表字段：名称、链接、备注、状态、排序值、创建时间
- 表单字段：
  - 名称（必填，最大15个中文字符）
  - 链接（必填，最大255字符，URL格式校验）
  - 备注（可选，最大127个中文字符）
  - 状态（必填，下拉选择：启用/禁用）
  - 排序值（可选，数字，默认0）

**路由配置：**
- 路径：`/blog/friend-link`
- 组件：`blog/BlogFriendLinkList`
- 权限：`blog:friend_link:list`（列表）、`blog:friend_link:create`（新增）、`blog:friend_link:update`（编辑）、`blog:friend_link:delete`（删除）

#### 12.2.2 管理后台-社交信息管理（`src/views/blog/BlogSocialInfoList.vue`）

**功能：**
- 使用 D2Table 组件实现列表、新增、编辑、删除
- 列表字段：名称、链接、备注、状态、排序值、创建时间
- 表单字段：
  - 名称（必填，最大15个中文字符）
  - 链接（必填，最大255字符，URL格式校验）
  - 备注（可选，最大127个中文字符）
  - 状态（必填，下拉选择：启用/禁用）
  - 排序值（可选，数字，默认0）

**路由配置：**
- 路径：`/blog/social-info`
- 组件：`blog/BlogSocialInfoList`
- 权限：`blog:social_info:list`（列表）、`blog:social_info:create`（新增）、`blog:social_info:update`（编辑）、`blog:social_info:delete`（删除）

#### 12.2.3 公共博客列表页优化（`src/views/public/BlogListPage.vue`）

**页面结构调整：**
```
┌─────────────────────────────────────────┐
│  Hero 区域（标题、搜索框）                │
├─────────────────────────────────────────┤
│  ┌──────────────┬─────────────────────┐ │
│  │  左侧主内容区  │  右侧侧边栏         │ │
│  │              │                     │ │
│  │  ┌─────────┐ │  ┌───────────────┐ │ │
│  │  │标签筛选  │ │  │  友情链接      │ │ │
│  │  └─────────┘ │  └───────────────┘ │ │
│  │              │                     │ │
│  │  ┌─────────┐ │  ┌───────────────┐ │ │
│  │  │文章列表  │ │  │  社交信息      │ │ │
│  │  │(置顶标识)│ │  └───────────────┘ │ │
│  │  └─────────┘ │                     │ │
│  │              │                     │ │
│  └──────────────┴─────────────────────┘ │
│  分页组件                                  │
└─────────────────────────────────────────┘
```

**功能点：**

1. **标签筛选区域：**
   - 位置：主内容区顶部，搜索框下方
   - 组件：使用 `el-tag` 或 `el-checkbox-group` 实现标签选择
   - 交互：点击标签后，调用 `publicBlogArticleList` 接口，传入 `tagId` 参数
   - 数据来源：调用 `publicBlogTagList` 接口获取标签列表

2. **文章列表置顶标识：**
   - 在文章卡片上显示「置顶」标签（如红色背景的「置顶」文字或置顶图标）
   - 置顶文章自动排在列表最前面（后端已处理排序）
   - 样式：置顶标签使用醒目的颜色（如红色、橙色），与普通标签区分

3. **友情链接展示区域：**
   - 位置：右侧侧边栏（PC端）或底部区域（移动端）
   - 组件：使用 `el-link` 或自定义链接组件
   - 数据来源：调用 `publicBlogFriendLinkList` 接口获取启用的友情链接
   - 样式：链接列表，每个链接显示名称，点击跳转到目标URL（新窗口打开）

4. **社交信息展示区域：**
   - 位置：右侧侧边栏（PC端）或底部区域（移动端）
   - 组件：使用图标 + 链接的组合（如 GitHub、Gitee、邮箱图标）
   - 数据来源：调用 `publicBlogSocialInfoList` 接口获取启用的社交信息
   - 样式：图标链接列表，每个社交信息显示图标和名称，点击跳转到目标URL（新窗口打开）

**移动端适配：**
- 侧边栏内容在移动端移至底部
- 标签筛选在移动端使用横向滚动或折叠面板
- 友情链接和社交信息在移动端使用更紧凑的布局

### 12.3 API 对接

#### 12.3.1 管理后台接口

**友情链接管理：**
- `blogFriendLinkList`：列表查询
- `blogFriendLinkCreate`：创建
- `blogFriendLinkUpdate`：更新
- `blogFriendLinkDelete`：删除

**社交信息管理：**
- `blogSocialInfoList`：列表查询
- `blogSocialInfoCreate`：创建
- `blogSocialInfoUpdate`：更新
- `blogSocialInfoDelete`：删除

**文章置顶：**
- `blogArticleTop`：设置置顶
- `blogArticleUntop`：取消置顶

#### 12.3.2 公共接口

**友情链接列表：**
- `publicBlogFriendLinkList`：获取启用的友情链接列表

**社交信息列表：**
- `publicBlogSocialInfoList`：获取启用的社交信息列表

**标签列表：**
- `publicBlogTagList`：获取启用的博客标签列表（用于标签筛选）

**文章列表扩展：**
- `publicBlogArticleList`：支持 `tagId` 参数（标签筛选），返回结果包含 `isTop` 字段

### 12.4 开发步骤与进度跟踪

> 下列任务将在方案评审通过后，按顺序实施；每个任务完成后需要在本小节及「## 5. 待实现 / 待完善功能」中标记完成，并与后端文档同步。

- **阶段十二：博客模块扩展功能前端实现（友情链接、社交信息、文章置顶、标签筛选）**

  - [✅] 12.4.1 生成 TypeScript 接口代码：
    - [✅] 等待后端完成 API 开发并执行 `scripts/generate-ts.sh` 生成 TypeScript 代码。
    - [✅] 在 `src/api/blog.ts` 中封装友情链接、社交信息、标签列表、置顶相关接口。

  - [✅] 12.4.2 实现管理后台-友情链接管理页面：
    - [✅] 创建 `src/views/blog/BlogFriendLinkList.vue`。
    - [✅] 使用 D2Table 组件实现列表、新增、编辑、删除功能。
    - [✅] 表单校验：名称、URL、备注长度校验（与后端保持一致）。
    - [✅] 路由配置：在 `src/router/index.ts` 中添加路由（`/blog/friend-link`）。

  - [✅] 12.4.3 实现管理后台-社交信息管理页面：
    - [✅] 创建 `src/views/blog/BlogSocialInfoList.vue`。
    - [✅] 使用 D2Table 组件实现列表、新增、编辑、删除功能。
    - [✅] 表单校验：名称、URL、备注长度校验（与后端保持一致）。
    - [✅] 路由配置：在 `src/router/index.ts` 中添加路由（`/blog/social-info`）。

  - [✅] 12.4.4 优化公共博客列表页（`BlogListPage.vue`）：
    - [✅] **标签筛选功能：**
      - [✅] 在 Hero 区域下方添加标签筛选组件。
      - [✅] 调用 `publicBlogTagList` 接口获取标签列表。
      - [✅] 实现标签选择交互（单选）。
      - [✅] 标签选择后，更新 `query.tagId`，重新调用 `publicBlogArticleList` 接口。
    - [✅] **文章置顶标识：**
      - [✅] 在文章卡片上添加置顶标识（红色「置顶」标签）。
      - [✅] 根据返回的 `isTop` 字段判断是否显示置顶标识。
      - [✅] 置顶文章样式优化（边框高亮、背景色区分）。
    - [✅] **友情链接展示：**
      - [✅] 在右侧侧边栏（PC端）或底部区域（移动端）添加友情链接区域。
      - [✅] 调用 `publicBlogFriendLinkList` 接口获取友情链接列表。
      - [✅] 实现链接展示和点击跳转（新窗口打开，URL 自动补全 `https://`）。
      - [✅] 移动端适配：侧边栏内容移至底部。
    - [✅] **社交信息展示：**
      - [✅] 在右侧侧边栏（PC端）或底部区域（移动端）添加社交信息区域。
      - [✅] 调用 `publicBlogSocialInfoList` 接口获取社交信息列表。
      - [✅] 实现图标链接展示（根据名称匹配图标，如 GitHub、Gitee、邮箱等）。
      - [✅] 点击跳转到目标URL（新窗口打开，URL 自动补全 `https://`）。
      - [✅] 移动端适配：侧边栏内容移至底部。
    - [✅] **页面布局调整：**
      - [✅] 调整 `BlogListPage.vue` 的布局结构，支持左右分栏（PC端）或上下布局（移动端）。
      - [✅] 保持现有的暖色渐变背景和卡片样式。
      - [✅] 确保移动端响应式布局正常（已修复移动端布局问题）。

  - [✅] 12.4.5 文章管理页面扩展（后台）：
    - [✅] 在 `src/views/blog/BlogArticleList.vue` 中添加置顶操作按钮。
    - [✅] 实现置顶/取消置顶功能（调用 `blogArticleTop`、`blogArticleUntop` 接口）。
    - [✅] 列表显示置顶标识（置顶文章显示「置顶」标签）。

  - [✅] 12.4.6 样式优化：
    - [✅] 友情链接和社交信息区域样式设计（与公共页面风格一致）。
    - [✅] 置顶标识样式设计（醒目但不突兀）。
    - [✅] 标签筛选组件样式设计（与搜索框风格一致）。
    - [✅] 移动端响应式样式优化（已修复移动端布局问题）。

  - [✅] 12.4.7 前后端联调测试：
    - [✅] 与后端开发人员联调，确保所有接口功能正常。
    - [✅] 测试标签筛选功能。
    - [✅] 测试置顶功能（置顶数量限制，已修复缓存问题）。
    - [✅] 测试友情链接和社交信息展示。
    - [✅] 测试移动端响应式布局。

  - [✅] 12.4.8 文档更新：
    - [✅] 更新本进度文档，标记已完成功能。
    - [✅] 更新后端文档，同步前端进度。

### 12.5 关键代码位置

**页面组件：**
- `admin-frontend/src/views/blog/BlogFriendLinkList.vue`
- `admin-frontend/src/views/blog/BlogSocialInfoList.vue`
- `admin-frontend/src/views/public/BlogListPage.vue`（优化）
- `admin-frontend/src/views/blog/BlogArticleList.vue`（扩展）

**API 封装：**
- `admin-frontend/src/api/blog.ts`（扩展）

**路由配置：**
- `admin-frontend/src/router/index.ts`

**样式文件：**
- `admin-frontend/src/styles/public-list.scss`（可能需要扩展）

---

## 11. 博客页面改造方案（标准博客风格）

### 11.1 改造目标

将当前的博客列表页和详情页改造成标准博客风格，参考主流技术博客的设计，提升用户体验和视觉效果。

### 11.2 参考设计分析

#### 11.2.1 桌面端博客列表页特点
1. **顶部导航栏**
   - 左侧：博客标题/Logo + 分类导航（主页、好玩的Java、中间件、数据、生活等）
   - 右侧：搜索框 + GitHub图标 + 其他功能图标
   - 导航栏固定在顶部，背景为白色或浅色

2. **左侧分类导航侧边栏**
   - 可折叠的分类树形结构
   - 当前选中项高亮显示
   - 支持多级分类展开/收起

3. **中间文章列表区域**
   - 卡片式布局，每篇文章一个卡片
   - 卡片内容：
     - 标题（可带置顶图标 📌）
     - 摘要/描述（2-3行截断）
     - 元信息：作者头像/名称、发布日期、阅读时间（约X分钟）
     - 标签（小标签形式）
   - 置顶文章有特殊标识（橙色标签"置顶"）
   - 卡片间距适中，有阴影效果

4. **右侧侧边栏**
   - 个人信息卡片（头像、昵称、简介）
   - 文章统计（X 文章、X 时间轴）
   - 社交链接（GitHub、Bilibili、微信等图标）
   - 友情链接
   - 其他功能入口

5. **分页组件**
   - 位于文章列表底部
   - 显示总条数、每页条数选择、页码导航

#### 11.2.2 移动端博客列表页特点
1. **顶部栏**
   - 左侧：汉堡菜单图标 + 头像
   - 右侧：搜索图标 + GitHub图标 + 菜单图标
   - 固定在顶部

2. **文章列表**
   - 单列布局
   - 卡片更紧凑
   - 左图右文布局（封面图固定宽度，高度约84px）
   - 标题、摘要、元信息垂直排列

3. **底部导航**
   - 可选：底部固定导航栏

#### 11.2.3 桌面端文章详情页特点
1. **左侧分类导航**
   - 与列表页一致的分类导航
   - 当前文章所属分类高亮

2. **中间文章内容区域**
   - 文章标题（大号字体）
   - 元信息：作者、日期、字数（约X字）、阅读时间（大约X分钟）
   - "此页内容"按钮（展开/收起目录）
   - 文章正文（Markdown渲染）
   - 广告位（可选）
   - 评论区域
   - 编辑链接、最近更新时间、贡献者信息
   - 上一页/下一页导航

3. **右侧目录侧边栏**
   - "此页内容"标题
   - 文章目录（自动生成，支持锚点跳转）
   - 目录项可点击，滚动到对应位置
   - 当前阅读位置高亮

#### 11.2.4 移动端文章详情页特点
1. **单栏布局**
   - 标题
   - 元信息
   - 文章正文
   - 评论区域
   - 上一页/下一页导航

2. **目录功能**
   - 可通过"此页内容"按钮展开/收起
   - 浮层或抽屉形式显示

### 11.3 改造方案

#### 11.3.1 博客列表页（BlogListPage.vue）改造

**布局结构调整：**
```
┌─────────────────────────────────────────────────────────┐
│  顶部导航栏（固定）                                        │
│  [Logo/标题] [分类1] [分类2] ... [搜索] [GitHub] [菜单]   │
├──────────┬──────────────────────────────┬───────────────┤
│          │                               │               │
│  左侧    │     中间文章列表区域            │   右侧        │
│  分类    │                               │   侧边栏      │
│  导航    │  ┌─────────────────────┐     │   - 个人信息  │
│  (可选)  │  │ 文章卡片1 (置顶)     │     │   - 文章统计  │
│          │  └─────────────────────┘     │   - 社交链接  │
│          │  ┌─────────────────────┐     │   - 友情链接  │
│          │  │ 文章卡片2            │     │               │
│          │  └─────────────────────┘     │               │
│          │  ┌─────────────────────┐     │               │
│          │  │ 文章卡片3            │     │               │
│          │  └─────────────────────┘     │               │
│          │  [分页组件]                  │               │
├──────────┴──────────────────────────────┴───────────────┤
│  底部（ICP备案信息）                                       │
└─────────────────────────────────────────────────────────┘
```

**具体改造点：**

1. **顶部导航栏（新增）**
   - 创建 `BlogHeader.vue` 组件
   - 包含：博客标题/Logo、分类导航、搜索框、社交图标
   - 固定在页面顶部，滚动时保持可见
   - 背景：白色，有轻微阴影

2. **左侧分类导航（可选，根据需求）**
   - 如果不需要分类导航，可以隐藏
   - 如果需要，创建 `BlogCategoryNav.vue` 组件
   - 支持树形结构，可折叠

3. **中间文章列表区域（优化）**
   - 移除当前的 Hero 区域（标题、搜索栏）
   - 文章卡片样式优化：
     - 卡片背景：白色
     - 卡片阴影：更柔和
     - 卡片圆角：8-12px
     - 卡片间距：16-20px
   - 文章卡片内容：
     - 封面图：16:9 比例，圆角顶部
     - 置顶标识：右上角橙色标签"置顶"
     - 标题：字体加粗，2行截断
     - 摘要：2-3行截断，灰色文字
     - 元信息：作者、日期、阅读时间（需要计算）
     - 标签：小标签，浅色背景
   - 移除标签筛选区域（移到顶部导航或侧边栏）

4. **右侧侧边栏（重构）**
   - 个人信息卡片：
     - 头像（圆形）
     - 昵称
     - 简介
     - 文章统计（X 文章）
   - 社交信息：
     - 图标形式展示（GitHub、Bilibili等）
     - 点击跳转
   - 友情链接：
     - 列表形式
     - 点击新窗口打开

5. **分页组件（优化）**
   - 样式更简洁
   - 显示总条数、每页条数、页码

6. **移动端适配**
   - 顶部导航：汉堡菜单 + 搜索
   - 文章列表：单列，左图右文
   - 侧边栏：可收起或移到底部

#### 11.3.2 博客详情页（BlogDetailPage.vue）改造

**布局结构调整：**
```
┌─────────────────────────────────────────────────────────┐
│  顶部导航栏（固定，与列表页一致）                           │
├──────────┬──────────────────────────────┬───────────────┤
│          │                               │               │
│  左侧    │     中间文章内容区域            │   右侧        │
│  分类    │                               │   目录        │
│  导航    │  文章标题                      │   侧边栏      │
│  (可选)  │  元信息（作者、日期、字数等）   │   - 此页内容  │
│          │  [此页内容] 按钮               │   - 目录列表  │
│          │                               │   - 当前高亮  │
│          │  文章正文（Markdown）          │               │
│          │                               │               │
│          │  [广告位]（可选）              │               │
│          │                               │               │
│          │  评论区域                     │               │
│          │                               │               │
│          │  [编辑] [更新时间] [贡献者]    │               │
│          │  [< 上一页] [下一页 >]         │               │
├──────────┴──────────────────────────────┴───────────────┤
│  底部（ICP备案信息）                                       │
└─────────────────────────────────────────────────────────┘
```

**具体改造点：**

1. **顶部导航栏（复用列表页）**
   - 使用相同的 `BlogHeader.vue` 组件
   - 保持导航一致性

2. **左侧分类导航（可选）**
   - 与列表页一致
   - 当前文章所属分类高亮

3. **中间文章内容区域（优化）**
   - 文章标题：
     - 字体：28-32px，加粗
     - 颜色：深色
     - 行高：1.3
   - 元信息栏：
     - 作者头像/名称
     - 发布日期
     - 字数统计（约X字）
     - 阅读时间（大约X分钟）
     - 标签
   - "此页内容"按钮：
     - 位于元信息下方
     - 点击展开/收起目录
     - 移动端：浮层显示
   - 文章正文：
     - Markdown 渲染
     - 代码块样式优化
     - 图片自适应
     - 表格样式优化
   - 广告位（可选）：
     - 位于文章中间或底部
     - 标注"广告"标识
   - 评论区域：
     - 评论数量
     - 评论输入框
     - 评论列表（如果后端支持）
   - 编辑信息：
     - "编辑此页"链接
     - 最近更新时间
     - 贡献者信息
   - 导航按钮：
     - 上一页/下一页
     - 显示相邻文章标题

4. **右侧目录侧边栏（新增）**
   - 创建 `BlogTOC.vue` 组件（Table of Contents）
   - 功能：
     - 自动从 Markdown 内容提取标题（H1-H6）
     - 生成目录树
     - 支持锚点跳转
     - 滚动时高亮当前阅读位置
     - 可折叠/展开
   - 样式：
     - 固定在右侧
     - 背景：白色或浅色
     - 目录项：可点击，当前项高亮

5. **移动端适配**
   - 单栏布局
   - 目录：通过"此页内容"按钮展开浮层
   - 文章内容：字体、间距优化

### 11.4 技术实现要点

#### 11.4.1 新增组件

1. **BlogHeader.vue** - 顶部导航栏
   - Props: 无
   - Events: search, category-change
   - 功能：导航、搜索、社交链接

2. **BlogCategoryNav.vue** - 左侧分类导航（可选）
   - Props: categories, currentCategory
   - Events: category-select
   - 功能：分类树、折叠/展开

3. **BlogTOC.vue** - 文章目录
   - Props: content (Markdown内容)
   - Events: anchor-click
   - 功能：提取标题、生成目录、锚点跳转、滚动高亮

4. **BlogAuthorCard.vue** - 作者信息卡片
   - Props: authorInfo
   - 功能：显示作者信息、统计

5. **BlogSocialLinks.vue** - 社交链接
   - Props: socialLinks
   - 功能：显示社交图标、跳转

#### 11.4.2 样式文件

1. **创建 `admin-frontend/src/styles/blog.scss`**
   - 博客页面通用样式
   - 导航栏样式
   - 文章卡片样式
   - 目录样式

2. **更新 `public-list.scss`**
   - 移除暖色渐变背景（改为白色或浅灰）
   - 优化卡片样式
   - 优化分页样式

3. **更新 `public-detail.scss`**
   - 优化文章内容样式
   - 添加目录样式
   - 优化移动端样式

#### 11.4.3 功能增强

1. **阅读时间计算**
   - 根据文章字数计算阅读时间
   - 公式：字数 / 300（中文）或 200（英文）
   - 显示：大约X分钟

2. **字数统计**
   - 从 Markdown 内容提取纯文本
   - 统计中文字数和总字符数
   - 显示：约X字

3. **目录生成**
   - 解析 Markdown 内容
   - 提取 H1-H6 标题
   - 生成层级结构
   - 添加锚点ID

4. **滚动高亮**
   - 监听滚动事件
   - 计算当前视口位置
   - 高亮对应的目录项

5. **相邻文章导航**
   - 需要后端提供接口：获取上一篇文章、下一篇文章
   - 或前端根据当前文章ID计算

### 11.5 实施步骤

#### 阶段一：基础布局改造（1-2天）
1. 创建 `BlogHeader.vue` 组件
2. 重构 `BlogListPage.vue` 布局
3. 移除 Hero 区域，添加顶部导航
4. 优化文章卡片样式
5. 重构右侧侧边栏

#### 阶段二：详情页改造（1-2天）
1. 创建 `BlogTOC.vue` 组件
2. 重构 `BlogDetailPage.vue` 布局
3. 添加目录功能
4. 优化文章内容样式
5. 添加元信息（字数、阅读时间）

#### 阶段三：功能增强（1天）
1. 实现阅读时间计算
2. 实现字数统计
3. 实现目录自动生成
4. 实现滚动高亮
5. 实现相邻文章导航（如果后端支持）

#### 阶段四：移动端适配（1天）
1. 移动端导航栏
2. 移动端文章列表
3. 移动端详情页
4. 移动端目录浮层

#### 阶段五：测试与优化（1天）
1. 功能测试
2. 样式调整
3. 性能优化
4. 兼容性测试

### 11.6 注意事项

1. **保持响应式设计**
   - 桌面端：三栏布局（左侧导航 + 中间内容 + 右侧目录）
   - 移动端：单栏布局，目录浮层

2. **性能优化**
   - 目录生成：使用虚拟滚动（如果目录项很多）
   - 图片懒加载
   - 代码分割

3. **SEO优化**
   - 语义化HTML
   - 合理的标题层级
   - Meta标签

4. **可访问性**
   - 键盘导航支持
   - 屏幕阅读器支持
   - 颜色对比度

5. **兼容性**
   - 现代浏览器支持
   - 移动端浏览器测试

### 11.7 参考资源

- Element Plus 组件库文档
- Markdown 解析库：`markdown-it` 或 `marked`
- 目录生成：`markdown-it-toc` 或自定义解析
- 滚动监听：`Intersection Observer API`

### 11.8 待确认事项

1. 是否需要左侧分类导航？
2. 是否需要广告位？
3. 是否需要评论功能？（需要后端支持）
4. 是否需要编辑功能？（需要权限控制）
5. 相邻文章导航是否需要后端接口支持？
