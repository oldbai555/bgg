## 2026-07-13（续）：Phase 3 第三篇（`21-cd-and-deployment.md`，六容器镜像 + docker-compose）前置工作完成

用户明确要求本篇只做**前置工作**（代码/配置落地），真实部署动作留到用户后续实际上线时自己调整（不是本次会话代跑）。

**核查发现：六个 Dockerfile 已经在 Phase 2 各服务拆分时落地，不是本次新增**——`admin-server/Dockerfile`（gateway，Phase 1 `09-ci-cd-and-deployability.md` 产出）+ `services/{iam,content,chat,task,sdk}/Dockerfile`（Phase 2 五次服务拆分各自带的产出），六份结构一致（`golang:1.24-alpine` 多阶段构建 → `alpine:latest` 运行时，`CGO_ENABLED=0`），符合 `21-cd-and-deployment.md` 第 3 节"不是 Phase 3 一次性新增六个"的预期，本轮未改动。**与文档规划的路径命名有出入，已按实际结构调整**：文档写的是 `cmd/gateway/Dockerfile`，但实际 gateway 就是 admin-server 仓库根的 `admin.go` 主包（没有独立 `cmd/gateway/` 目录，`cmd/` 目录实际只有 `adminseed`），所以 CI/compose 里 gateway 一律指向仓库根 `admin-server/Dockerfile`，五个 RPC 服务指向 `services/<name>/Dockerfile`。

**本次新增三块**：
1. **`.github/workflows/ci.yml` 新增 `build-images` matrix job**：`needs: [lint-build, unit-test]`（对齐现有 job 实际命名，不是文档里的 `lint-and-build`），`if: github.event_name == 'push' && github.ref == 'refs/heads/main'`（只在推主分支时构建推送镜像，`feature/**` push 和 PR 只跑门槛 job，不产出镜像——文档只写"每次 push 到主分支触发"，这个限制条件是本次落地时补充明确的）；六个服务用 `matrix.include` 显式列出 `service`+`dockerfile` 两个字段（而不是文档示例的字符串拼接 `services/${{matrix.service}}/Dockerfile`，因为 gateway 的 Dockerfile 路径不遵循这个规律）；镜像 tag `ghcr.io/${{ github.repository_owner }}/admin-<service>:${{ github.sha }}`，用 GitHub 表达式自动取仓库 owner（当前是 `oldbai555`，已用 `git remote -v` 核实），不是硬编码 `<user>`，仓库迁移账号时不需要改这段。
2. **`script/admin.sh` 新增 `compose` 子命令组**：逐字按文档骨架的 `compose_pull`/`compose_deploy`/`compose_status`/`compose_logs` 四个函数落地（`COMPOSE_REMOTE_HOST`/`COMPOSE_REMOTE_DIR` 两个环境变量、`COMPOSE_FILE` 固定 `docker-compose.prod.yml`），`main()` 新增和 `supervisor)` 平行的 `compose)` 分发，`usage()` 补充命令说明和示例。`bash -n` 语法检查通过，`compose status` 缺 host 参数时正确报错退出（原有 `build`/`package`/`supervisor` 等命令组行为未受影响）。
3. **新增 `docker-compose.prod.yml`**：六个服务（无 mysql/redis 容器——生产 MySQL/Redis 是已有外部实例，走环境变量/`-redis-config /etc/work/redis.json`，不容器化，这点和本地 `docker-compose.yml` 的关键差异）；`image:` 用 `ghcr.io/${GHCR_NAMESPACE:-oldbai555}/admin-<service>:${TAG:-latest}`（**踩坑记录**：一开始想用 YAML anchor `x-registry: &registry` + `"*registry-admin-gateway:..."` 复用镜像仓库前缀，实测这不是合法 YAML——alias `*registry` 必须是独立标量节点，不能拼进字符串中间，改成每个 service 直接写 `${GHCR_NAMESPACE:-oldbai555}` 环境变量插值，更简单也是 docker-compose 官方支持的标准写法）；gateway 挂载宿主机 `/etc/work:ro`（沿用 `-redis-config` 机制）+ `uploads` 命名卷（与 task 共享，同本地 compose 已有模式）；五个 RPC 服务的环境变量名与各自 `services/<name>/etc/<name>.yaml` 里 `${VAR}` 占位符逐一核对一致（如 `IAM_MYSQL_DSN`/`TASK_CALLBACK_ENDPOINT` 等），服务间地址用 compose 服务名（`iam:8081`/`task:8090`/…），和本地 `docker-compose.yml` 同一套命名。本机无 Docker，用 `ruby -ryaml` 做了两份 compose 文件 + `ci.yml` 的 YAML 语法校验（均通过），未做 `docker build`/`docker compose up` 实测。
4. **`docs/14-production-deployment-checklist.md` 新增条目 10**：记录本次三块新增的内容、用户切换到 compose 部署时的具体操作顺序（服务器准备 → `.env` 填值 → `compose pull` → `compose deploy` → 按条目 5-9 已有验证路径复核 → 确认无误再停 Supervisor），状态标 `已就绪，待生产环境实际部署时执行`。

**未做的事（按用户要求，留给用户后续实际部署时处理）**：真实 `docker build`/`docker compose up` 验证（本机无 Docker）；生产服务器上的 `docker login ghcr.io`、`.env` 真实取值、Supervisor→compose 的真实切换时机——这些都属于 `10-dev-execution-and-review-points.md` 第 2 节"触及生产环境部署动作本身"的必停项，AI 不代做。

`go build ./...` 通过（本篇只涉及 CI/脚本/compose 配置，无 Go 代码改动）。**Phase 3 三篇（`19`/`20`/`21`）至此全部完成，Phase 1-3 整体重构规划的三阶段主体工作按文档收官**，剩余都是需要用户在真实生产环境执行的部署动作。

**下一步**：按用户要求"最后一起提交"，本次会话不 `git commit`；等用户确认后统一提交 `20`/`21` 两篇的全部改动（`scripts/generate-swagger.sh`、`docs/openapi/`、`.github/workflows/ci.yml`、`script/admin.sh`、`docker-compose.prod.yml`、`docs/14-production-deployment-checklist.md`、`docs/progress.md`）。Phase 3 文档收尾还有一件遗留事项：`10-dev-execution-and-review-points.md` 开头提到的"是否要把开发期执行策略正式写回 `AGENTS.md`/`.cursor/rules`"，是 Phase 3 收尾时要向用户提出的决定点，本次未涉及，留给用户确认 Phase 1-3 整体收尾时一并决定。
