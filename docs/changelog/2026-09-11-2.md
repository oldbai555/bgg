# 2026-09-11-2 CI 自动部署前端到 bgg-dev

## 交接摘要（必读，写给下一个没有上下文的新会话）

**在做什么 / 目标**：

> merge 到 `main` 后，除现有后端六服务镜像部署外，CI 同步构建并部署 `admin-frontend` 静态包到 bgg-dev，飞书卡片一并展示前端部署状态，不再依赖本机手动 `script/deploy-frontend.sh`。

**已完成**：

> - [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) 新增 `deploy-frontend`：`lint-build` 成功后并行，Runner 上 pnpm build → scp → SSH 替换 `/home/work/service/admin-frontend/dist`（无 sudo）
> - `notify-feishu` 的 `needs` 增加 `deploy-frontend`，卡片新增「前端部署」行；总览全绿条件 = 镜像构建 + 后端部署 + 前端部署均 success
> - [`script/deploy-frontend.sh`](../../script/deploy-frontend.sh) 去掉交互 sudo，与 CI 远端替换逻辑对齐；目录不可写时提示 chown
> - 侧栏高亮相关前端改动（见 [`2026-09-11.md`](./2026-09-11.md)）会随本次前端自动部署一并上线

**当前状态（现在卡在哪 / 进行到哪一步）**：

> - **bgg-dev 属主已改为 `ubuntu:ubuntu`，目录可写（已核对 `touch` 通过）。**
> - 代码待合入 `main` 后 CD 才会跑通。

**下一步计划**：

> 1. ~~bgg-dev chown~~（已完成）
> 2. 开 PR 合入含本改动 + 侧栏高亮修复的分支到 `main`
> 3. 观察 Actions：`deploy-frontend` 与 `deploy-dev` 并行，飞书三态齐全且全绿
> 4. 打开 `https://oldbai.top/bgg/admin/dashboard` 确认新前端（含侧栏高亮修复）

**踩过的坑 —— 绝对不要再犯**：

> - 现象：本机 `deploy-frontend.sh` 用 `sudo` 换 dist，CI 的 deploy 密钥用户在 sudo 组但无免密。根因：GitHub Actions 无法交互输入 sudo 密码。正确做法：目录属主改为 `ubuntu`，CI/本地脚本都不走 sudo。
> - 现象：旧 changelog（2026-07-31）写「前端仍不自动部署」已过时。正确做法：以本文为准；合 main 后前后端均由 CI 部署。

---

## 上线需求：

无

## 技术优化：

| 序号 | 功能需求 | 前端 | 后端 | 测试 | 所属分支 | 代码是否合并 |
|---|---|---|---|---|---|---|
| 1 | CI 自动部署 admin-frontend | `deploy-frontend` job + `deploy-frontend.sh` | 无 | 合 main 后看 Actions + 飞书 | 当前工作区 | * [ ] |

## 线上缺陷：

无

## CI 自动部署前端

### 变更内容

```PlainText
前端：
- .github/workflows/ci.yml（deploy-frontend + notify-feishu）
- script/deploy-frontend.sh（去 sudo）

后端：无
```

### 上线前准备

#### bgg-dev 一次性（必须）

```bash
sudo chown -R ubuntu:ubuntu /home/work/service/admin-frontend
```

#### GitHub Secrets

复用已有：`DEPLOY_SSH_HOST` / `DEPLOY_SSH_USER` / `DEPLOY_SSH_KEY` / `FEISHU_WEBHOOK_URL`，无需新增。
