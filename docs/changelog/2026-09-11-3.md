# 2026-09-11-3 CI deploy-frontend：Node 升到 22

## 交接摘要（必读，写给下一个没有上下文的新会话）

**在做什么 / 目标**：

> #10 合入 main 后 `deploy-frontend` 失败：`pnpm@11` 要求 Node >= 22.13，workflow 仍写 Node 20，且 Actions 已弃用 Node 20。

**已完成**：

> - [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) `deploy-frontend` 的 `node-version`：`20` → `22`

**当前状态（现在卡在哪 / 进行到哪一步）**：

> 修复待合入 main；合入后应重跑 CD，观察 `deploy-frontend` 是否全绿。

**下一步计划**：

> 1. 合 PR 到 main
> 2. 确认 Actions `deploy-frontend` 成功、飞书前端部署行绿
> 3. 打开 `https://oldbai.top/bgg/admin/dashboard` 验证新包

**踩过的坑 —— 绝对不要再犯**：

> - 现象：`ERR_UNKNOWN_BUILTIN_MODULE: node:sqlite` + `pnpm requires at least Node.js v22.13`。根因：pnpm 11 绑定了 Node 内置 sqlite，Node 20 没有；CI 却 pin 了 20。正确做法：与 pnpm major 对齐 Node（此处用 22 LTS），不要在 Actions 继续用已弃用的 Node 20。

---

## 上线需求：

无

## 技术优化：

| 序号 | 功能需求 | 前端 | 后端 | 测试 | 所属分支 | 代码是否合并 |
|---|---|---|---|---|---|---|
| 1 | CI frontend Node 22 | `ci.yml` deploy-frontend | 无 | 合 main 后看 Actions | `fix/0911_ci-frontend-node22` | * [ ] |

## 线上缺陷：

> deploy-frontend job 因 Node/pnpm 版本不匹配失败
