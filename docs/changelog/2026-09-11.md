# 2026-09-11 侧栏菜单高亮：sqlpub path 前缀缺口 + 最长前缀匹配

## 交接摘要（必读，写给下一个没有上下文的新会话）

**在做什么 / 目标**：

> 修复后台侧栏「仪表盘」及多菜单（如文章管理）不高亮的问题。用户首次登录落在 `/admin/dashboard` 时侧栏不定位；文章编辑/保存后也常显示异常。

**已完成**：

> - 根因核实（SSH bgg-dev + 查 sqlpub）：前端 2026-07-16 已部署 `/admin/*` 路由；sqlpub `admin_menu.path` 仍是 `/dashboard`、`/blog/article` 等（41 条无 `/admin` 前缀），与登录跳转的 `/admin/dashboard` 错位；本机库早已迁完。侧栏 `default-active` 此前从未做子路由前缀匹配。
> - 从 git `8acf8a63` 恢复迁移脚本到 [`admin-server/db/services/iam/menu/migrations/add_admin_prefix_to_menu_path_20260714.sql`](../admin-server/db/services/iam/menu/migrations/add_admin_prefix_to_menu_path_20260714.sql)（cleanup `63de093f` 曾删掉整个 migrations 目录）。
> - 前端：新增 [`admin-frontend/src/utils/menuActive.ts`](../admin-frontend/src/utils/menuActive.ts)（type=2 最长前缀匹配）+ 单测；[`AppSidebar.vue`](../admin-frontend/src/components/layout/AppSidebar.vue) 用解析结果作 `default-active` 并加 `:key`；[`breadcrumb.ts`](../admin-frontend/src/utils/breadcrumb.ts) 复用同一套匹配。

**当前状态（现在卡在哪 / 进行到哪一步）**：

> - **sqlpub 迁移已由用户执行并核对通过**：仪表盘 `/admin/dashboard`、文章 `/admin/blog/article`，`legacy_cnt=0`。
> - 前端代码已改、单测/本机构建通过；**待** `script/deploy-frontend.sh` 发 bgg-dev（前端不随 CI 自动部署）。
> - 部署后需清浏览器 `localStorage` 的 `admin_menus` / `admin_cache_at`（或重新登录），否则 5min TTL 内仍可能用旧菜单 path。

**下一步计划**：

> 1. ~~用户在 sqlpub 执行迁移~~（已完成，`legacy_cnt=0`）
> 2. bgg-dev 一次性 `sudo chown -R ubuntu:ubuntu /home/work/service/admin-frontend`（见 [`2026-09-11-2.md`](./2026-09-11-2.md)；当前仍 root 属主，CI/手动部署都写不进去）
> 3. 合入含侧栏修复 + CI 前端自动部署的 PR 到 `main`（此后前端由 `deploy-frontend` job 自动发，不必再手动 `deploy-frontend.sh`）
> 4. 清菜单缓存或重新登录，验证仪表盘/文章编辑侧栏高亮

**踩过的坑 —— 绝对不要再犯**：

> - 现象：登录后 URL 是 `/bgg/admin/dashboard`，点「仪表盘」却变成 `/bgg/dashboard`，侧栏两边都不高亮，面包屑一个英文一个中文。根因：前端路由命名空间迁移只在本机库执行了 `admin_menu.path` 加 `/admin` 前缀，bgg-dev 用的 sqlpub 从未执行；前端却已按 `/admin/*` 部署。正确做法：本机改库 / 改前端路由时，同步核对 **sqlpub** 存量数据；迁移 SQL 不要只留在本机执行记录里。
> - 现象：即使 path 对齐后，进 `/admin/blog/article/edit/:id` 侧栏仍不亮。根因：`el-menu` 的 `default-active` 只做字符串精确等于 `route.path`，且文档语义是初始激活。正确做法：对 type=2 菜单做最长前缀匹配，并用 `:key="activePath"` 强制重建。

---

## 上线需求：

无

## 技术优化：

无

## 线上缺陷：

> 侧栏菜单高亮缺失（仪表盘首登、文章编辑后等）

|序号|功能需求|前端|后端|测试|所属分支|代码是否合并|
|---|---|---|---|---|---|---|
|1|侧栏/面包屑按菜单最长前缀高亮|`menuActive.ts` + AppSidebar + breadcrumb|恢复 path 前缀迁移 SQL|vitest `menuActive.spec.ts`|当前工作区|* [ ] |

## 变更版本/标题

### 变更内容

```PlainText
前端：
- src/utils/menuActive.ts / menuActive.spec.ts
- src/components/layout/AppSidebar.vue
- src/utils/breadcrumb.ts

后端：
- db/services/iam/menu/migrations/add_admin_prefix_to_menu_path_20260714.sql（从历史恢复，待在 sqlpub 执行）
```

### 上线前准备

#### 必须由用户执行

1. sqlpub 执行上述迁移 SQL（幂等）
2. `bash script/deploy-frontend.sh`
3. 清菜单缓存或重新登录后回归高亮
