## 2026-07-13：处理 Phase 2 收尾的两项已知遗留问题——`VideoUpdate` 字段绑定 bug 修复、`Login`/`Refresh`/`FileUpload`/`FileDownload`/`Notice` 真实环境端到端验证补充（docker 容器化实测按用户要求本轮不处理，留到真实上线时再做）

**1. `VideoUpdate` 字段绑定 bug 修复（`14-production-deployment-checklist.md` 条目 8 记录的遗留项）**：
- 根因确认：`video` 表 DDL 后来加了 `uuid`/`god_num`/`xlzz_urls`/`type` 四个字段（采集视频功能引入），但 `services/content/internal/model/video/videomodel_gen.go` 从未跟着用新 DDL 重新生成，`Insert`/`Update` 仍手写绑定旧的 6 个字段（`Name/Cover/Duration/PlayUrl/Description/DeletedAt`），与按 `Video` 结构体动态生成的 SET/VALUES 占位符数量不一致——`PUT /api/v1/videos` 从引入那次提交起就没有真正生效过。模板本身（`.template/model/{insert,update}.tpl`）没有问题，纯粹是"表结构改了、Model 没跟着重新生成"。
- 修复：用户执行 `scripts/generate-model.sh db/services/content/video/create_table_video.sql -d services/content/internal/model/video` 重新生成（第一次因为 AI 给错 `-d` 目录生成到了错误位置，已识别并清理误生成文件，第二次用正确目录重新生成，`videomodel.go` 自定义文件按预期被跳过未覆盖）。
- **连带影响**：重新生成后，新模板给 `video` 表的唯一索引 `uk_uuid` 生成了 index-cache 模式（新增 `FindOneByUuid`），同时移除了旧版本残留的 `FindPage`/`FindChunk` 两个方法（其余模型早就不生成这两个方法了，只有 video 因为长期没重新生成才一直留着）。`services/content/internal/repository/video/video_repository.go` 原来在无筛选条件时会调用已被移除的 `model.FindPage`，编译会报错——已改成统一走仓库层已有的 `findPageWithFilter`（squirrel 实现，本身就正确处理空筛选条件），无筛选时行为不变，顺手删掉了这个不必要的分支。`Video` 结构体字段顺序变化（`GodNum`/`Cover` 互换）确认不影响现有代码——仅有的两处 `videomodel.Video{}` 构造都用具名字段。
- 验证：`go build ./...`、`go vet ./...`、`go test ./...`、`golangci-lint run ./services/content/...` 全仓库全绿。

**2. `Login`/`Refresh`/`FileUpload`/`FileDownload`/`Notice` 真实环境端到端验证（`14-production-deployment-checklist.md` 条目 9 记录的遗留项，借用户远程库 `oldbai` + 本机 Redis，和前五轮同一个库）**：
- 起两个真实进程：iam-rpc（`IAM_MYSQL_DSN` 指向 oldbai）+ gateway（本地临时 JWT 密钥/Redis 配置，`IAM_RPC_ENDPOINT`/`IAM_CALLBACK_RPC_ENDPOINT` 指向 iam-rpc，其余四个 RPC endpoint 指向未占用端口，`NonBlock: true` 不阻塞启动，本轮不验证这几个跨服务调用），均成功连上真实 MySQL/Redis。
- 由于不知道真实管理员密码，新建了一个一次性测试账号（一次性 Go 脚本直接写库，bcrypt 哈希已知密码）+ 一次性测试角色（绑定 permission id=1"全部权限"通配符 bypass），验证完整清理，不使用/不触碰真实管理员账号。
- **`Login`**：测试账号登录成功签发 `accessToken`/`refreshToken`，`GET /api/v1/profile` 用 accessToken 正确返回 `permissions: ["*"]`，验证了 `AuthMiddleware`/`PermissionMiddleware` 通配符 bypass 全链路；同时验证了 Login 内部的 `createUnreadNoticeNotifications`——测试账号首次登录时正确为已发布的存量公告（"欢迎使用后台管理系统"）创建了一条未读通知，只影响该测试账号自己，不影响其他真实用户。
- **`Refresh`**：用 Login 拿到的 `refreshToken` 请求 `/api/v1/refresh`，正确签发新的 `accessToken`/`refreshToken`。
- **`FileUpload`/`FileDownload`**：`POST /api/v1/files/upload` 上传一个测试文本文件，正确落盘到共享 `uploads` 卷 + 通过 `IamRPC.FileRegister` 写入 `admin_file` 元数据；`GET /api/v1/files/download?id=X` 下载下来的内容和原始文件字节完全一致，验证了 `IamRPC.FileGetMeta` 元数据查询链路。
- **`Notice`/`Notification` 批量通知创建**：`POST /api/v1/notices` 创建一条 `status=2`（已发布）的测试公告，异步的 `createNotificationsForAllUsers` 正确为全部 3 个真实用户（`oldbai`/`admin`/测试账号）各创建了一条通知记录，验证了批量写入链路（这个远程库目前只有 3 个真实用户，写入面很小，验证后精确清理）。
- 验证完整清理：一次性 Go 脚本删除测试通知、测试账号的登录日志、测试角色+用户+关联的角色权限/用户角色绑定、测试公告及其触发的全部通知、测试文件元数据；本地磁盘的上传文件同步删除。清理后核对 `admin_user`/`admin_login_log`/`admin_notice`/`admin_notification`/`admin_file`/`admin_role`/`admin_user_role`/`admin_role_permission` 八张表行数精确回到验证前基线（`2/42/1/5/8/2/2/21`）。两个测试进程正常终止。

**3. 提交时本仓库的 pre-commit AI code review 钩子（Gentleman Guardian Angel）拦下并顺手修复了两类质量问题**（钩子对照 `AGENTS.md` 独立复核了本轮 diff，不是本轮主动排查发现的）：
- `video_repository.go` 的 `Create`/`FindByUuid` 在 Model 重新生成之后仍分别手写 squirrel INSERT/SELECT，绕开了 `CachedConn` 的缓存失效/索引缓存——`Create` 改回调用 `r.model.Insert`（现在已经正确绑定全部字段，不再需要手写绕过），`FindByUuid` 改回调用新生成的 `r.model.FindOneByUuid`；顺手删除了 `Create` 里"生成的 Insert 方法可能不包含所有新增字段"这条已经过时的注释。改动后 `services/content/internal/logic/logic_test.go` 的 `TestVideoCollect_DuplicateUuid` 需要同步更新 mock 的 SQL 匹配正则（squirrel 生成大写 `FROM`，goctl 模板生成小写 `from`，列顺序也随 `Video` 结构体字段顺序变化同步调整）。
- `docs/后端开发进度.md` 第 0 节目录结构、第 8 节关键代码位置多处仍停留在 iam-rpc 拆分前的 gateway 路径（`internal/repository/`、`internal/model/` 等已整体删除的目录），已按 iam-rpc 拆分后的实际路径逐条核实并改写（RBAC/系统支撑/日志与监控/性能与缓存优化/demo/公告与通知/性能日志/M3U8/打点统计），发现原 `internal/repository/sql_conn.go`（连接池调优）在拆分后未找到对应文件，如实标注为待核实项，不编造路径。
- 验证：`go build ./...`、`go vet ./...`、`go test ./...`、`golangci-lint run ./services/content/...` 再次全仓库全绿。
- 提交钩子第二轮复核又指出 `docs/后端开发进度.md` 第 8、17-20 节多处"关键代码位置"仍写着 `internal/rpcserver/{taskcallback,iamcallback}/`（iam-rpc 拆分前的单体内嵌路径，已整体搬迁到 `services/iam/internal/server/`，见第 21 节），以及第 12-14 节博客模块的代码位置仍是 content-rpc 拆分前的单体路径——按"只追加、不重写历史叙述，但索引类路径要指向现状"的原则，在对应位置追加了搬迁说明/交叉引用，不改写原有历史描述本身。

**遗留/需要用户关注的点**：
1. `docker compose up` 本身仍未做容器化实测——本轮按用户要求明确不处理，等真实上线时再做。
2. `04` 文档任务 6（`AuthDomainService.Login` 下沉）：优先级低，仍可继续推迟。
3. Phase 2 全部收尾遗留问题至此处理完毕（`VideoUpdate` bug 已修复，`Login`/`Refresh`/`FileUpload`/`FileDownload`/`Notice` 已补验证），只剩 docker 容器化实测留给真实上线阶段。下一步是 Phase 3（可观测性/CI-CD），或者先做一次全量回顾/用户复核，取决于用户判断。
4. 本次改动尚未 `git commit`。

**下一步**：Phase 2 五个服务拆分 + 收尾遗留问题（`VideoUpdate` bug、`Login`/`Refresh`/`FileUpload`/`FileDownload`/`Notice` 真实验证）全部完成。建议下一次会话和用户对齐 Phase 3 范围与优先级。
