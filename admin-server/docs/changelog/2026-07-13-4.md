## 2026-07-13：Phase 3 第二篇（`20-api-docs-generation.md`，`generate-swagger.sh`）完成

**产出**：
1. 新增 `admin-server/scripts/generate-swagger.sh`，逐字按 `20-api-docs-generation.md` 第 2.3 节给出的骨架落地——`GOCTL_BIN`/新增 `GOCTL_SWAGGER_BIN` 两段式解析、颜色输出、位置参数默认 `api/admin.api`、`-h/--help`、`y/N` 确认交互，风格与 `generate-model.sh`/`generate-ts.sh` 逐字一致；`chmod +x` 补可执行位（Week 1 已发现的"新脚本默认无执行位"问题，这次新建时直接给对）。
2. `go install github.com/zeromicro/goctl-swagger@latest` 装到 `$(go env GOPATH)/bin`（`v0.2.0`）；按 `10-dev-execution-and-review-points.md` 第 1 条第 1 款（`generate-swagger.sh` 已被明确列入开发期可直接执行的脚本清单），本次由 AI 直接执行了一次生成，不是仅落地脚本代码。
3. 生成 `docs/openapi/admin-api.json`（101 个 path，268KB），`jq .` 校验合法 JSON；抽查 `/api/v1/login` 的请求体 `LoginReq`（`username`/`password` 两个必填字符串字段）与 `admin.api` 源定义完全一致，`/api/v1/dict/batch` 等真实路由均正确出现。

**已知限制（工具本身的，不是本次引入的 bug）**：`admin.api` 里唯一的 `map[string]T` 字段——`DictBatchGetResp.dicts map[string]DictGetResp`（`api/admin.api:528`）——生成时 `goctl-swagger` 打印 `暂不支持map类型` 并产出一个指向不存在定义 `mapstringDictGetResp` 的悬空 `$ref`（`definitions` 里没有这个 key）。这是 `goctl-swagger` 上游对 Swagger 2.0 `map` 类型支持不完整导致的，不是 `generate-swagger.sh` 脚本或 `admin.api` 定义的问题；除这一个字段外，抽查的其余 definitions/paths 均正确。本轮按 `20-api-docs-generation.md` 选型评估的既定结论（接受 `goctl-swagger` 现状，不因个别边界情况另起工具链），不处理，记录在此供后续如果有人真的要看这个字段的文档时知道去源码核对。

**未做的事（按文档第 3、4 节明确排除）**：未给 `iam-rpc`/`content-rpc`/`chat-rpc`/`task-rpc`/`sdk-rpc` 任何一个 RPC 服务新增文档生成脚本或依赖；`sdk-rpc` 的可交互 Swagger UI 是文档里点名的"未来触发条件成立后才做"事项，本轮不做。

**待用户决定**：`docs/openapi/` 是否提交进 git——本次会话未 `git add`，目录和生成的 JSON 都是未跟踪状态，`20-api-docs-generation.md` 建议提交但不强制。

**下一步**：Phase 3 剩余最后一篇 `21-cd-and-deployment.md`（docker-compose 生产切换 + 按服务 CI 镜像构建）。

---
